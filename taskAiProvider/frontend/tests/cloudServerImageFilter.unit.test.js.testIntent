# cloudServerImageFilter.unit.test.js 测试意图

## 1. 测试目标
- 对应功能意图：登记云平台服务器镜像时，镜像下拉菜单支持按关键词过滤。
- 验证目标：过滤函数对名称、ID、系统字段、架构字段执行大小写不敏感匹配，并在空关键词时完整返回。

## 2. 关键断言
- 关键词为空（含仅空白）时，返回原镜像列表，不丢项。
- 输入名称关键词时，仅返回名称匹配项。
- 输入镜像 ID 片段时，仅返回 ID 匹配项。
- 输入操作系统版本或架构关键词时，可正确匹配对应项。
- 匹配规则必须大小写不敏感。

## 3. 典型破坏信号
- 过滤后列表始终为空或始终为全量。
- 名称可匹配但 ID/架构不可匹配。
- 关键词大小写改变导致结果不一致。

## 4. 回归验证要点
- 运行 `npm run test:unit -- cloudServerImageFilter.unit.test.js` 应全部通过。
- 若后续扩展过滤字段，需同步补充此测试文件断言与本意图文档。
