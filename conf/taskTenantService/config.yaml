host: 0.0.0.0
port: 8020
enabled: true
logging:
  log_file: logs/task-tenant-service/task-tenant-service.log

services:
  taskTenantService:
    host: "0.0.0.0"
    port: 8020
  taskAuth:
    host: "127.0.0.1"
    port: 8003
  taskProjectService:
    host: "127.0.0.1"
    port: 8016
  taskCloudService:
    host: "127.0.0.1"
    port: 8018
shared:
  # MUST be Django loopback — NEVER public gateway (${subdomains.api}).
  internalSecret: ""
frontendBase: "${scheme}://${subdomains.www}"
