# Grafana/Loki Web UI 地址。默认 127.0.0.1（同节点）；多节点部署时设置 INFRA_HOST=<IP>。
# 参见 .ai/01_project_constraints/39_network_topology_aware_config.md
host: ${INFRA_HOST:-10.2.150.68}
port: 3000
logging.log_file: logs/infra/ai-monitor.log
