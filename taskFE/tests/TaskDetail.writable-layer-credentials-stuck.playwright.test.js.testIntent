# 测试意图：任务关联可写层因克隆凭证不齐卡住

| ID | 场景 | 期望 |
|----|------|------|
| E1 | mock 评论容器 running + 端点已注册 + empty pending layer-graph + clone-log `REPO_CLONE_CREDENTIALS_INCOMPLETE` + SSE 双向心跳 ok | `comment-layer-ztree-panel` 可见；其内 `[data-testid=comment-layer-ztree-loading-error]` 含「Git 授权未齐」，不含「等待可写层就绪」，无转圈 spinner，无「正在准备可写层」 |

对应测例：`TaskDetail.writable-layer-credentials-stuck.playwright.test.js`
