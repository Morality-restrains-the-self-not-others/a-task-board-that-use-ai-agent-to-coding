// @ts-check
/**
 * 看板纵轴已改为进度列（每列一进度），交付物改由顶栏过滤表达。
 * 跨交付物拖拽不再作为看板纵轴操作；本用例保留为显式 skip，避免误报回归。
 */
import { test } from '@playwright/test';

test.describe('工作面板 跨交付物分栏拖拽', () => {
  test('看板不再以交付物为纵轴，跨交付物拖拽用例退役', async () => {
    test.skip(
      true,
      '2026-07-15：纵轴=进度列（每列仅一进度）；交付物由过滤栏表达，不再跨交付物列拖拽',
    );
  });
});
