测试目标：
- 当系统策略关闭手机号登录时，登录页必须隐藏所有手机号登录入口，防止前端入口绕过策略。

关键业务断言：
- `手机号/密码` 与 `手机号/验证码` 两个切换按钮不可见。
- 移动端快捷入口 `使用短信验证码登录` 也不可见。
- 邮箱登录入口仍可见，保证基础登录链路可用。

典型破坏信号：
- 页面仍出现任一手机号入口按钮。
- 策略接口返回关闭，但 UI 未回退到邮箱登录模式。

回归验证要点：
- 运行 `Login.phone-login-policy-toggle.playwright.test.sh`，确认入口显隐与策略一致。

