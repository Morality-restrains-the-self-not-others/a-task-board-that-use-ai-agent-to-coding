# Onboarding.unauthenticated-redirect-to-login.playwright.test.js

## 意图
验证未登录（无有效会话）用户访问 `/onboarding/` 必须被重定向到登录页，不得停留在「创建公司」表单。

## 被测功能
1. 重定向：无任何会话 cookie 访问 /onboarding/ → 跳转 /auth/login/（roles API 401 → 前端守卫跳转）
2. 回跳参数：重定向 URL 携带 `next=/onboarding/`，登录后可返回引导页继续创建公司
3. 登录页渲染：跳转后登录表单（data-alias="cmp-login-form"）可见

## 前置条件
- 前端 Vite dev server 运行中（`taskFE/app`，端口按 conf 配置）
- task-gateway + taskAuth 运行中（未登录访问 `/api/auth/user-roles/` 返回 401）

## 测试环境变量
- `PLAYWRIGHT_SITE_ORIGIN` (默认: from port_config.json)
- 无需测试账号（未登录场景）

## 预期结果
测试通过：未登录访问 /onboarding/ 重定向到 /auth/login/?next=/onboarding/ 且登录表单可见。
