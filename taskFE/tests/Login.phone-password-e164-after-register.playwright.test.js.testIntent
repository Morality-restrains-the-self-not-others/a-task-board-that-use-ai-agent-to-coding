测试目标：
- 手机号注册后（auth_login_method 存 national + phone_country_calling_code），
  立即用同一 E.164（+86 前缀）+ 密码登录，断言离开 /auth/login/。
- 回归 OPT-20260819-035：此前 handleLogin 对 E.164 精确匹配 identifier（无国码）失败，
  返回「手机号或密码错误」。

关键业务断言：
- 真实网关 POST /api/auth/ 接受 { phone: '+8613901234567', password } 并返回 token
  （服务端 E.164 规范化已生效）。
- 登录页「手机号/密码」表单提交的 phone 为完整 E.164（+86 前缀 + 国内号），
  而非仅 national 11 位。
- 登录成功后 URL 离开 /auth/login/。

典型破坏信号：
- 服务端仍按 identifier 精确匹配 E.164 → 登录返回「手机号或密码错误」。
- 前端仅提交 national（无 +86 国码）→ 服务端无法识别国码，即使规范化也无从解析。

回归验证要点：
- 运行 `Login.phone-password-e164-after-register.playwright.test.sh`，
  确认两个用例（真实网关 + 登录页表单）全绿。
- 服务端逻辑另由 taskAuth `TestHandleLoginPhonePasswordAcceptsE164AfterRegister` 覆盖。
