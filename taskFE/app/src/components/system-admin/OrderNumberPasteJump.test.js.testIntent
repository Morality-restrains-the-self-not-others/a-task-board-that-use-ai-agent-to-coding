# OrderNumberPasteJump.test.js 测试意图

## 测试范围
交易单号查询子组件：空输入不 emit；非空 trim 后 emit `search`。

## 关键业务断言
- 空输入提示「请输入交易单号」且不发 search
- 有内容时 emit 规范化后的查询串
- 微信导出前导反引号被剥离

## 破坏信号
空输入仍 emit，或未 trim。

## 回归验证
在 vitest 环境下运行本文件。
