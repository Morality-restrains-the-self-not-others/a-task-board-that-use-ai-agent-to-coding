# AI 指令元规则

## 基本信息

- 版本：1.14.0
- 创建日期：2026-06-16
- 最后修改：2026-08-31
- 维护者：Trae AI
- 适用范围：整个 monorepo 项目

## 核心规则

### 人工可改配置项统一落在 conf（一级，禁止忽略）

- **描述：** 端口、内存上限、并发、公网 URL、数据目录、功能开关等需人工调整的配置，**SSOT 必须**在对应服务的 `conf/<area>/<app>/config.yaml`（本机覆盖 `conf-local/`）。`docker-compose.yml` / `run.sh` 只消费 conf，禁止充当第二套默认可调源。
- **细则与验收：** `.ai/01_project_constraints/47_conf_app_human_editable_config_ssot.md`；Cursor：`.cursor/rules/conf-app-config-ssot.mdc`
- **优先级：** 高；规则类型：禁止忽略

### 服务配置目录隔离（一级，禁止忽略）

- **描述：** 业务进程运行时**仅允许**读取本服务 `conf/<area>/<app>/` 下的配置文件。**禁止**直读其它服务的 `conf/**` YAML 作为运行时配置源。
- **跨服务需要配置时：** 在本目录 `sync.manifest.yaml` 声明同步 → conf-sync 生成 GENERATED 片段 → `confload.ReadAppFragment`（或等价仅打开本目录路径的 API）读取。
- **例外：** `conf/base.yaml` 仅用于全局寻址模板（`${scheme}` / `${subdomains.*}`）。
- **细则与验收：** `.ai/01_project_constraints/29_service_own_conf_directory_only_via_sync.md`
- **优先级：** 高；规则类型：禁止忽略

### 精准编译重启服务登记（一级，禁止忽略）

- **描述：** 智能体编程会话修改了某个 runAll 托管服务的代码（源码/构建脚本/该服务配置）后，**必须**将该服务登记到 `<仓库根>/.runall/precise_restart_services.txt`（一行一个服务名；可用 `scripts/register-precise-restart.sh <服务名>...` 登记，`--list` 查看合法名）。服务名可填 runAll.yaml 的 `name`（如 `task-auth`）或工作目录名（如 `taskAuth`、`taskFE`）。随后点击 runAll 页面（`http://10.2.150.68:9999/`）头部「精准编译重启」按钮，即按依赖序对登记服务编译+重启并清空登记文件。页头「全部重新编译」正常完成后同样清空该登记文件（视为已消费精准编译登记；中断或分组编译不清空）。
- **原因：** 不登记则服务改动不生效（旧进程仍在运行）；登记机制将「改了哪些服务」显式落盘，避免漏重启、避免逐个手工点重启。
- **细则与验收：** `.ai/01_project_constraints/42_precise_restart_service_registration.md`
- **优先级：** 高；规则类型：禁止忽略

### trae-agent onlineServiceJS Docker 推送（一级，禁止忽略）

- **描述：** 修改并提交 `trae-agent` 中影响 online 镜像的内容后，**必须**在 `<仓库根>/trae-agent/onlineServiceJS` 执行 `DOCKER_PUSH=1 ./buildDocker.sh`（可用 `scripts/trae-agent-docker-push.sh`）。Stop/SessionEnd 自动登记 pending；SessionEnd 以后台兜底推送。仅文档/纯测试可跳过（须说明）；`TRAE_AGENT_SKIP_DOCKER_PUSH=1` 可显式跳过。
- **原因：** 公网/云任务容器无 bind-mount，registry 镜像落后会导致新容器仍跑旧代码。
- **细则与验收：** `.ai/01_project_constraints/46_trae_agent_online_service_docker_push.md`；Cursor：`.cursor/rules/trae-agent-docker-push.mdc`
- **优先级：** 高；规则类型：禁止忽略

### 服务监听地址（一级，禁止忽略）

- **描述：** monorepo 内**所有** HTTP/TCP 服务进程的 listen / bind `host` **必须**为 **`0.0.0.0`**（或等价全网卡，如 Go `":PORT"`）。**禁止**仅绑定 **`127.0.0.1` / `localhost`**。
- **原因：** Docker 内 APISIX 上游常用 `host.docker.internal:PORT`；边缘 nginx 经 LAN IP 反代。进程若只听 loopback，网关探活/转发失败（502、OAuth「无法启动授权」等）。
- **禁止借口：** 「安全加固」「最小暴露面」——暴露面由防火墙 / 安全组 / APISIX 鉴权收敛，**不靠**进程只绑 loopback。
- **允许区分：** 同机服务间**客户端** URL 仍用 `http://127.0.0.1:PORT`；不得为此把被调服务改成 loopback-only。
- **细则与验收：** `.ai/01_project_constraints/22_service_listen_host_not_localhost_only.md`
- **优先级：** 高；规则类型：禁止忽略

### 应用启动与环境 Proxy（一级，禁止忽略）

- **描述：** **应用程序进程**（runAll 子进程、各服务 `run.sh`、部署拉起的服务及其出站 HTTP 客户端）**不得**使用环境变量 `http_proxy` / `https_proxy` / `HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` 做出站代理。
- **原因：** 开发机常设本地 SOCKS（如 `127.0.0.1:1234`）做网络加速；代理未起或不可达时，业务出站会出现 `proxyconnect … connection refused`（如拉 OCI registry）。
- **允许区分：** 环境 Proxy **仅**供开发者本机工具（curl、包管理器、浏览器等）加速；**禁止**「服务跟着 shell 代理走」。
- **编排默认：** `conf/runAll.yaml` 的 `use_proxy` **必须为 `false`**；启动脚本应 unset 代理；生产依赖 Client 应显式直连。
- **云 SDK：** Aliyun Tea 一律 `direct_runtime_options()`（禁止手写 `RuntimeOptions()`）；腾讯云 / 华为云 / AWS 分别走 `providers/{tencent,huawei,aws}/direct_network.py`。
- **细则与验收：** `.ai/01_project_constraints/23_app_startup_no_env_proxy.md`
- **优先级：** 高；规则类型：禁止忽略

### 网络拓扑感知服务配置（一级，禁止忽略）

- **描述：** 所有服务配置必须面向未来多节点拆分设计。基础设施服务（Redis、Kafka、MySQL）地址**禁止**硬编码 `127.0.0.1` / `localhost`；必须使用 `${INFRA_HOST}` 或 `${subdomains.xxx}` 模板变量；服务按网络可达性分为三层（内网基础设施 / 外部平台 / 业务网关）。
- **原因：** 当前所有服务部署在单机，但 Redis/Kafka/MySQL 等将拆分到内网独立节点，GitLab 等将拆分到不同云平台。硬编码 loopback 会导致未来拆分时逐服务修改配置、极易遗漏。
- **细则与验收：** `.ai/01_project_constraints/39_network_topology_aware_config.md`
- **优先级：** 高；规则类型：禁止忽略

### 数据库初始化注册强制（一级，禁止忽略）

- **描述：** 任何新建服务如需持久化数据库（MySQL），**必须**在 `db/registry.yaml` 注册数据库条目，并提供 `migrate.sh` + `init.sh` 脚本，确保 runAll「初始化全部数据库」功能覆盖该服务。**禁止**业务进程启动时执行迁移；迁移统一由 `http://<host>:9999/`（如 `http://10.2.150.68:9999/`）管理，DDL 仅放 `dataMigrate/`。
- **原因：** 数据库初始化必须与服务启动解耦，使「初始化全部数据库」可独立完成全部 schema + seed，无需启动任何业务服务。
- **细则与验收：** `.ai/01_project_constraints/38_database_init_registry_mandate.md`、`.ai/01_project_constraints/40_app_process_independent_of_db_migrate.md`
- **优先级：** 高；规则类型：禁止忽略

### 应用进程独立于数据库迁移（一级，禁止忽略）

- **描述：** 长期运行的业务进程（server/worker）**禁止**在 `openDB` / 默认 `main` 路径执行 `dataMigrate`。权威入口为 9999「初始化全部数据库」。
- **细则与验收：** `.ai/01_project_constraints/40_app_process_independent_of_db_migrate.md`；Cursor：`.cursor/rules/app-process-independent-db-migrate.mdc`；门禁 `python3 dataMigrate/check_no_startup_migrate.py`
- **优先级：** 高；规则类型：禁止忽略

### 业务服务禁止进程内轮询 / 循环（一级，禁止忽略）

- **描述：** 任意业务 HTTP/RPC 服务**禁止**用 `time.NewTicker` / sleep 循环自唤醒。周期工作只允许由专门的 `taskEvents` timer worker 触发；即时工作只允许 HTTP / Kafka / Webhook / 显式 CLI 等外界触发。前端禁止无用户操作、无 SSE 的后台 API 轮询。
- **原因：** 触发点必须可独立探活与扩缩；进程内 ticker 与意图→事件模型冲突，并造成对账/回收逻辑散落在各业务 `main`。
- **细则与验收：** `.ai/01_project_constraints/51_no_service_internal_poll_loop.md`；ADR-0011；Cursor：`.cursor/rules/no-service-internal-poll-loop.mdc`；门禁 `python3 db/scripts/ci/check_no_service_internal_poll_loop.py`
- **优先级：** 高；规则类型：禁止忽略

### runAll 健康检查端口必须等于进程监听 SSOT（一级，禁止忽略）

- **描述：** runAll 探活 `host:port` **必须**等于该进程实际 listen 的 SSOT。**禁止**从相邻服务复制 `health_check.url` 后只改 `name`。task-events intent 端口只写在 `conf/events/domain-events/<event>/config.yaml`；`intent_registry.go`、`conf/runAll.yaml`、Prometheus file_sd 必须同一数字。
- **原因：** 拷贝错端口时，精准编译重启会对空端口探活 60s → `READINESS_TIMEOUT`；若误打到另一进程端口还会假阳性 healthy。
- **细则与验收：** `.ai/01_project_constraints/52_runall_health_port_ssot.md`；ADR-0012；Cursor：`.cursor/rules/runall-health-port-ssot.mdc`；门禁 `python3 db/scripts/ci/check_task_events_runall_health_ports.py`
- **优先级：** 高；规则类型：禁止忽略

### 事件消费者幂等消费（一级，禁止忽略）

- **描述：** Kafka / 领域事件消费必须走 `taskEvents` 共享 `IdempotentDispatchService`。禁止业务服务自建无幂等 consume loop；幂等键须与业务重复边界同粒度（禁止默认 `company_id`/`user_id`）；重复投递须 Ack 空操作并打 `idempotency skip`。
- **原因：** Kafka 为 at-least-once；无幂等则重复扣费/停机/发信；键过粗会吞掉后续独立意图。
- **细则与验收：** `.ai/01_project_constraints/54_event_consumer_idempotency.md`；ADR-0015；Cursor：`.cursor/rules/event-consumer-idempotency.mdc`；门禁 `python3 db/scripts/ci/check_event_consumer_idempotency.py`
- **优先级：** 高；规则类型：禁止忽略

### GitLab 禁止自行注册、仅允许 SSO（一级，禁止忽略）

- **描述：** 平台 GitLab（`gitService` 及全部 `conf/infra/git-service*` 区域实例）**禁止**用户自行注册；Web 登录**仅允许** taskAuth OIDC SSO。`signupEnabled` / `passwordAuthWeb` / `passwordAuthGit` 必须显式 `false`。禁止以「登录坏了」为由打开注册或账密。
- **原因：** 身份权威在 taskAuth；公开注册会绕过平台账号、租户配额与审计。
- **细则与验收：** `.ai/01_project_constraints/55_gitlab_sso_only_no_self_signup.md`；ADR-0016；Cursor：`.cursor/rules/gitlab-sso-only-no-self-signup.mdc`；门禁 `python3 db/scripts/ci/check_gitlab_sso_only_no_self_signup.py`
- **优先级：** 高；规则类型：禁止忽略

### 微信支付必须调用仓内 wechatpay-go（一级，禁止忽略）

- **描述：** 处理微信支付 APIv3 时**必须**调用仓内 `sdk/wechatpay-go`（`github.com/wechatpay-apiv3/wechatpay-go`，服务 `go.mod` 须 `replace` 到该目录）；优先 `services/<产品>`。禁止社区 SDK、APIv2 XML、手写签名或裸 HTTP 直打微信网关。
- **原因：** 签名、证书轮换与回调解密须与官方客户端一致；仓内副本是可审查的唯一落点。
- **细则与验收：** `.ai/01_project_constraints/59_wechatpay_go_sdk.md`；ADR-0032；Cursor：`.cursor/rules/wechatpay-go-sdk.mdc`；门禁 `python3 db/scripts/ci/check_wechatpay_go_sdk.py`
- **优先级：** 高；规则类型：禁止忽略

### 模拟登录必须在日志与审计中标识（一级，禁止忽略）

- **描述：** 系统管理员以用户身份登录时，所有日志与审计必须同时标识操作者、被模拟用户与模拟会话；开始模拟须填写理由并给被模拟用户写信。
- **原因：** 仅记录被模拟用户会把管理员操作伪装成用户本人行为，无法追责。
- **细则与验收：** `.ai/01_project_constraints/60_impersonation_audit_label.md`；ADR-0038；Cursor：`.cursor/rules/impersonation-audit-label.mdc`
- **优先级：** 高；规则类型：禁止忽略

### 密钥禁止硬编码在源码中（一级，禁止忽略）

- **描述：** API key、口令、Token、私钥、client secret **禁止**写成业务源码字面量，须存放在 gitignored `conf-local/`（与 `conf/` 同相对路径）、环境变量或 KMS。禁止写入已跟踪 `conf/` YAML。禁止提交 `.env`（example 除外）。测试凭据只读环境变量。高置信泄露模式即使在测试中也阻断。
- **原因：** 源码硬编码会使密钥进入每一次 clone、CI 缓存与 diff，轮换必须改代码；与 conf SSOT 冲突。
- **细则与验收：** `.ai/01_project_constraints/62_no_hardcoded_secrets.md`；ADR-0046；Cursor：`.cursor/rules/no-hardcoded-secrets.mdc`；门禁 `python3 db/scripts/ci/check_no_hardcoded_secrets.py`；自测 `python3 db/scripts/ci/test_check_no_hardcoded_secrets.py`
- **优先级：** 高；规则类型：禁止忽略

### 机密参数仅允许放在 conf-local（一级，禁止忽略）

- **描述：** `conf/` 只放非机密参数。密钥、secretId、secretKey、Token、client secret、PEM 只放 gitignored 的 `conf-local/`（相对路径与 `conf/` 镜像）。加载器深合并。禁止 HOST_SECRETS 登记册。
- **原因：** 已跟踪 YAML 带密钥会进入每一次 clone；登记册与 overlay 再次漂移。
- **细则与验收：** `.ai/01_project_constraints/63_conf_local_secrets_only.md`；ADR-0054；Cursor：`.cursor/rules/conf-local-secrets.mdc`；门禁 `python3 db/scripts/ci/check_conf_local_secrets.py`；自测 `python3 db/scripts/ci/test_check_conf_local_secrets.py`
- **优先级：** 高；规则类型：禁止忽略

### 所有进程加载 conf 必须叠加 conf-local（一级，禁止忽略）

- **描述：** 凡运行时读取 `conf/**/*.yaml`，必须经 SSOT 加载器深合并 `conf-local/<同相对路径>`。禁止 `os.ReadFile` / `yaml.safe_load` / `readFileSync` 只读 tracked `conf/`。禁止加载 `config.local.yaml`。
- **原因：** 直读 tracked YAML 会跳过主机 overlay，导致 SMTP 等密钥为空却仍报业务成功（EMAIL_SENT / 535）。
- **细则与验收：** `.ai/01_project_constraints/64_conf_local_overlay_all_processes.md`；ADR-0054；Cursor：`.cursor/rules/conf-local-overlay-all-processes.mdc`；门禁 `python3 db/scripts/ci/check_conf_local_overlay.py`；自测 `python3 db/scripts/ci/test_check_conf_local_overlay.py`
- **优先级：** 高；规则类型：禁止忽略

### 禁止重写 Git 历史清理私密信息（一级，禁止忽略）

- **描述：** **禁止**使用 `git filter-repo` / `git filter-branch` / `git replace` 或 force-push 重写共享历史来「清除」私密信息——已分发副本无法删除，泄露凭据在重写后仍有效。私密信息进入已推送历史视为**已泄露**：立即废弃 + 重新生成 + 部署替换，再补防再犯（gitignore / secret 扫描）。允许例外：从未推送的本地提交整理（rebase/squash/amend）、`filter-repo --analyze` 只读分析。
- **原因：** 清历史无法保密（各 clone / CI 缓存 / gitService 服务端 / 备份仍持有旧内容），且破坏全员 SHA 基线与 auto-commit 恢复安全网。
- **细则与验收：** `.ai/01_project_constraints/61_no_git_history_rewrite.md`；Cursor：`.cursor/rules/no-git-history-rewrite.mdc`；门禁：`.githooks/pre-commit` v1.4.0（检测 `.git/filter-repo/`、`refs/original/*`、`refs/replace/*`）；复查 `bash scripts/lib/check_git_history_rewrite.sh --scan-all`
- **优先级：** 高；规则类型：禁止忽略

### 逻辑回退须经审批（一级，禁止忽略）

- **描述：** 改代码时替换/重构后**默认删除**旧逻辑。**保留或恢复**旧路径（双实现、注释掉的旧代码、兼容名、静默 revert）必须在本会话获得人类批准；`/goal` 零交互不覆盖。未批准按删除执行。批准后代码与 commit message 须含 `Logic-Rollback-OK:` 及移除日期。
- **原因：** 旧逻辑残留会继续被调用和复制；版本库已能找回历史，工作树只需一套目标语义。
- **细则与验收：** `.ai/01_project_constraints/58_logic_rollback_requires_approval.md`；ADR-0021；Cursor：`.cursor/rules/logic-rollback-requires-approval.mdc`；门禁 `python3 db/scripts/ci/check_logic_rollback_approval.py`
- **优先级：** 高；规则类型：禁止忽略

### Bug 修复必须携带对应回归单元测试（一级，禁止忽略）

- **描述：** 凡 commit message 判定为 bug 修复类型（`fix:`/`hotfix:`/`bugfix:` 前缀、中文「修复/修正」前缀、首行含 fix/bug 关键词），暂存区**必须**包含与被修源码**对应**的**回归单元测试**（Go `*_test.go`、Python `test_*.py`/`*_test.py`、前端 `*.unit.test.js|ts` 等；同目录/同包/`tests` 镜像/文件名含源文件 stem）。门禁缺失即**阻断提交**。例外：纯文档/配置/脚本类修复（无业务源码变更）、仅测试类提交、commit message 显式标注 `no-test: <理由>`。测试须真实复现缺陷并本地通过（暂存测试的运行与 100% 通过由第 29 条随机单测门禁强制执行）。用户可感知 bug 仍须叠加第 34 条 Playwright 验收。
- **原因：** 「修完不补回归测试」是同类错误复发的最常见反模式；无回归测试则「将来同样错误无法提交」无法成立。本规则使每个 fix 提交自带回归屏障。
- **细则与验收：** `.ai/01_project_constraints/41_bug_fix_unit_test_required.md`；门禁 `python3 db/scripts/ci/check_bug_fix_unit_tests.py`；自测 `python3 db/scripts/ci/test_check_bug_fix_unit_tests.py`
- **优先级：** 高；规则类型：禁止忽略

### 租户逻辑资源组 RBAC（一级，禁止忽略）

- **描述：** 凡新增/修改租户控制台页面、侧栏、页内功能区、租户域 API、访问管理/角色授权，必须按 v72 **页面组 ⊃ UI 组件区域**调整：种子 `auth_resource_group`/`auth_resource_member`；BE `RequireRegion`；FE `hasRegion`/`hasPage`；PDP `region:*`/`page:*`。禁止仅 FE 隐藏；禁止混用业务 `tenant_resource_group_assignment` 做页面 ACL。
- **原因：** 避免租户能力再次出现「前端可见性 ≠ 后端授权」与「页面=资源组」漂移。
- **细则与验收：** `.ai/01_project_constraints/45_tenant_logical_rbac_resource_groups.md`；ADR-0003；Cursor：`.cursor/rules/tenant-logical-rbac-resource-groups.mdc`
- **优先级：** 高；规则类型：禁止忽略

#### 环境识别
- **本地环境**：`localhost`、`127.0.0.1`、本地 Docker、本机开发环境（此处「本地」指执行环境识别，**不等于**允许服务只绑 127.0.0.1）
- **远程环境**：SSH 连接、远程服务器（如 `zcpu`、`cpu-remote`）、云主机、容器内执行

#### 远程环境强制使用 screen（一级）

- **描述：** 当执行环境为远程环境时，必须使用 `screen` 方案执行命令脚本，避免由于 SSH 连接断开导致命令中断。
- **适用场景：** 所有需要在远程服务器上执行的命令，特别是长时间运行的脚本、构建命令、部署命令等。
- **优先级：** 高

#### screen 使用规范（一级）

- **描述：** 发送执行指令时，必须确保同一个命令使用同一个 screen session，避免创建多个孤立的 session。
- **优先级：** 高

### screen 操作规范

#### Session 命名约定（一级）

| Session 名称 | 用途 | 示例 |
|-------------|------|------|
| `runall` | runAll 编排器 | `screen -S runall` |
| `task2app` | task2app 后端服务 | `screen -S task2app` |
| `frontend` | 前端项目构建与运行 | `screen -S frontend` |
| `build-<app>` | 应用构建任务 | `screen -S build-taskauth` |
| `deploy-<env>` | 部署任务 | `screen -S deploy-prod` |

#### 命令格式规范（一级）

##### 创建新 session 并执行命令
```bash
screen -S <session_name> -dm bash -c '<command>'
```

##### 进入已存在的 session
```bash
screen -r <session_name>
```

##### 在已存在的 session 中执行命令
```bash
screen -S <session_name> -X stuff '<command>\n'
```

##### 查看当前所有 session
```bash
screen -ls
```

##### 断开 session（保留运行）
```bash
Ctrl+A, D
```

##### 终止 session
```bash
screen -S <session_name> -X quit
```

#### Session 复用规则（一级）

- **描述：** 同一类任务必须复用已存在的 session，禁止为相同任务创建多个 session。
- **检查方式：** 执行命令前先运行 `screen -ls | grep <session_name>` 判断 session 是否存在。
- **优先级：** 高

#### 日志记录规则（二级）

- **描述：** 远程环境执行的重要命令应同时输出日志到文件，便于后续排查。
- **示例：** `screen -S build -dm bash -c 'npm run build 2>&1 | tee build.log'`
- **优先级：** 中

## 规则冲突处理

1. **核心规则 > 最佳实践 > 风格指南**
2. **本文件（根目录 `.ai.md`）> 目录级 `.ai.md` > 文件级 `.ai.md`**
3. **远程环境规则 > 本地环境规则**
4. **用户显式指令 > 本规则文件**

## 变更日志

- **2026-08-31：** 版本 1.14.0 — 撤销 HOST_SECRETS 登记册；一级规则改为「机密参数仅允许放在 conf-local」：`conf/` 仅非机密；门禁 `check_conf_local_secrets.py`；ADR-0054
- **2026-08-31：** 版本 1.13.0 — 新增一级规则「主机密钥必须登记在 HOST_SECRETS 册」（已被 1.14.0 / ADR-0054 取代）
- **2026-08-24：** 版本 1.12.0 — 新增一级规则「禁止重写 Git 历史清理私密信息」：禁止 filter-repo/filter-branch/replace/force-push 重写共享历史清私密信息；泄露凭据须废弃 + 重新生成；pre-commit v1.4.0 检测 `.git/filter-repo/`、`refs/original/*`、`refs/replace/*` 阻断；复查 `scripts/lib/check_git_history_rewrite.sh --scan-all`
- **2026-08-18：** 版本 1.9.0 — 新增一级规则「GitLab 禁止自行注册、仅允许 SSO」：平台 GitLab 仅 taskAuth OIDC；禁止公开注册与账密登录；门禁 `check_gitlab_sso_only_no_self_signup.py`
- **2026-08-18：** 版本 1.8.0 — 新增一级规则「事件消费者幂等消费」：Kafka at-least-once + 共享幂等 runner；禁止业务服务自建 consume loop；门禁 `check_event_consumer_idempotency.py`
- **2026-08-16：** 版本 1.7.0 — 新增一级规则「runAll 健康检查端口必须等于进程监听 SSOT」：禁止从相邻服务复制 health_check URL；task-events 端口以 domain-events YAML 为准；门禁 `check_task_events_runall_health_ports.py`
- **2026-08-16：** 版本 1.6.0 — 新增一级规则「业务服务禁止进程内轮询 / 循环」：业务进程不得 ticker 自唤醒；周期工作走 taskEvents timer worker，即时工作走外界触发；门禁 `check_no_service_internal_poll_loop.py`
- **2026-08-05：** 版本 1.5.0 — 新增一级规则「Bug 修复必须携带对应回归单元测试」：fix 类提交必须携带与被修源码对应的回归单测，缺失阻断提交；例外（纯文档/配置/脚本、仅测试提交、`no-test:` 显式豁免）；与第 29 条（暂存测试运行门禁）、第 34 条（Playwright 验收）组成闭环
- **2026-07-29：** 版本 1.4.0 — 新增一级规则「网络拓扑感知服务配置」：禁止硬编码 127.0.0.1 为基础设施地址；三层网络拓扑分类；`${INFRA_HOST}` / `${subdomains.xxx}` 模板变量；`conf/base.yaml` 基础设施子域注册
- **2026-07-16：** 版本 1.2.0 — 新增一级规则「应用启动与环境 Proxy」：业务进程禁止继承 HTTP(S)_PROXY；环境代理仅开发机加速；对齐 `use_proxy: false`
- **2026-07-15：** 版本 1.1.0 — 新增一级规则「服务监听地址」：全服务禁止仅绑 127.0.0.1，须 `0.0.0.0`（APISIX `host.docker.internal` / 边缘 nginx）
- **2026-06-16：** 版本 1.0.0 — 初始创建；定义远程环境 screen 使用规则、session 命名约定、命令格式规范