# 意图：服务器运行状态按评论容器归属（无评论区全局 header）

## 目标

任务详情「服务器运行状态」挂在**对应评论**的「执行细节 → 服务器运行状态」Tab（见 `comment_execution_details`），不在评论列表顶端再放全局「镜像运行状态」卡片。

## 业务意图 → 事件对照

| 业务意图 | 事件名（过去式） | MQ类型/契约 | 发布点 | 消费者/副作用 | 例外理由 |
|---------|----------------|------------|--------|--------------|---------|
| 运行状态按评论归属展示 | — | — | — | — | 无对应事件：纯前端展示，无服务端业务状态变更 |

## 变更记录

| 日期 | 相对旧版 | 原因 |
|------|----------|------|
| 2026-08-22 | 执行细节 summary 徽章在 Released 时显示「服务器已释放」 | binding=`running` 与运行态已释放不一致 |
| 2026-08-17 | 实例详情补带宽计费模式与公网出/入带宽（EIP 回退） | 运行态网格缺少流量带宽 |
| 2026-08-15 | 去掉评论列表顶端 `image-runtime-entry` 全局 header，并删除 `buildImageRuntimeEntries` 管道 | 与执行细节内启动日志/SSE/运行态重复，占用 Feed 顶部 |
| 2026-08-11 | 运行态/内容/历史请求失败文案挂 data-traceId；runtime JSON 注入 trace_id |
| 2026-08-11 | runtime-status：授权缺失回退 CSC 缓存/同平台授权 | 启动态「已启动」与运行态「未知+未找到授权」矛盾 |

## 非目标

- 每评论独立云实例 API（见 OPT-20260722-038）
- 在评论 Feed 顶部恢复镜像/SSE/启动日志总览卡
