# 分支策略编辑：双输入合并


## 业务意图 → 事件对照

> 存量回填（自动）：对照 `.ai/08_prompt_management/01_intent_driven_development.md`。事件名若为启发式占位，可在后续迭代精修。

**无对应事件**：纯前端展示/交互或设计治理，无服务端业务状态变更意图。

| 业务意图 | 事件名（过去式） | 发布点 | 消费者/副作用 | 例外理由 |
|---------|----------------|--------|--------------|---------|
| 分支策略编辑：双输入合并 | — | — | — | 纯前端展示/交互或设计治理，无服务端业务状态变更意图 |
## 变更记录

- 2026-07-12：工作分支模板+工作分支、目标分支模板+目标分支，各自合并为单一输入框（datalist 承载模版/共有分支候选）。

## 功能意图

1. 任务详情「分支策略」编辑态仅两个输入：
   - **工作分支**：一个 input，datalist 提供模版展开后的候选名，也可手改。
   - **目标分支**：一个 input，datalist 提供各仓库共有分支，也可手改。
2. 不再单独展示「工作分支模板」「目标分支模板」下拉。
3. 创建任务弹窗保持同样的「各一个输入框」形态。

## 验收

- 编辑态可见输入控件数量：工作分支 1 + 目标分支 1（不含刷新按钮）。
- 从工作分支 datalist 选模版后填入完整分支名，并正确推断 preset。
- 目标分支 datalist 仍为共有分支交集。
