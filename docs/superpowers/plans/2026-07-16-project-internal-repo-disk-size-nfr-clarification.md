# NFR：内部仓磁盘占用展示

日期：2026-07-16  
默认等级：L2（Standard）

| 类别 | 等级 | 说明 |
|------|------|------|
| 性能 | L2 | 内部仓并行拉取；成功结果进程内短缓存 60s；单仓失败不拖垮详情；外部仓零远端调用 |
| 可用性 | L2 | statistics 失败 → 省略字段，详情仍 200 |
| 安全 | L3 | 仅当前用户 token；不查外部仓；日志脱敏 |
| 可观测 | L2 | warn/info 记录 internal 判定与 size 获取成败 |
| 一致性 | L2 | 实时值；不保证与 GitLab UI 秒级同步 |

## 领域模型影响

无新聚合；响应 DTO 增量字段。不改 `project_repos` 表。
