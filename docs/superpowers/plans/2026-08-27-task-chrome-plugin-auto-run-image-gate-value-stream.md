# 价值流：Chrome 插件自动运行与已安装镜像挂钩

- **Date:** 2026-08-27
- **Increment:** 单一垂直切片（选镜像 ↔ 自动运行控件 + 提交门禁）

## Related Value Streams

- `2026-08-27-task-chrome-plugin-project-single-select-auto-run-value-stream.md`：项目单选 + 按项目能力钳制自动运行。该流明确「不补镜像前端门禁」。本增量在其上补镜像挂钩，不撤销项目能力门禁。

## 端到端

已登录用户打开浮窗或 DevTools → 选工作空间与（可自动运行的）项目 → 未选镜像时「是否自动运行」不可勾选并提示先选镜像 → 选择已安装镜像后自动运行启用（默认勾选，可取消）→ 创建任务；若仍 `auto_run` 且无镜像，客户端拦截，不发请求。

## 增量

| ID | 切片 | 用户可见价值 | 测试点 |
|----|------|--------------|--------|
| Inc-1 | 镜像必选与自动运行挂钩 | 勾选自动运行前必须选已安装镜像；不自动运行可不选镜像 | T1–T10 见测试意图 |

无第二增量：不改 API、不补硬件模版 UI。

## YAML

不写入根 `value-stream.yaml`（与上一 chrome 插件 UI 切片一致：无后端字段变更）。
