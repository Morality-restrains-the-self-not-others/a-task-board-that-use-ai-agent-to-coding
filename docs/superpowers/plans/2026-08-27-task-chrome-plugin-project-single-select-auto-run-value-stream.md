# 价值流：Chrome 插件项目单选与自动运行联动

- **Date:** 2026-08-27
- **Increment:** 单一垂直切片（选择项目 → 自动运行控件状态）

## Related Value Streams

- `2026-08-27-task-chrome-plugin-project-auto-run-label-value-stream.md`：列表名称旁徽章。本增量在其上把多选改为单选，并把徽章所表达的能力接到「是否自动运行」控件。不撤销徽章。

## 端到端

已登录用户打开浮窗或 DevTools → 选择工作空间 → GET 项目列表 → **单选**一个项目（名称旁仍有可/不可自动运行）→ 下方「是否自动运行」随该项目是否允许自动运行启用/禁用并更新提示 → 创建任务 payload 至多一个 `project_id`。

## 增量

| ID | 切片 | 用户可见价值 | 测试点 |
|----|------|--------------|--------|
| Inc-1 | 单选 + 自动运行联动 | 选不可自动运行的项目时无法勾选自动运行；可选时默认勾选且可取消 | T1–T12 `project-auto-run-label.test.js` + 使用说明 |

无第二增量：不改创建 API、不补镜像/硬件模版前端门禁。
