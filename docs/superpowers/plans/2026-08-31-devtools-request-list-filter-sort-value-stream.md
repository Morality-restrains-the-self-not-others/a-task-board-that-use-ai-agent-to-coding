# 价值流：DevTools 请求列表过滤与排序

- **Date:** 2026-08-31
- **Increment:** 单一垂直切片

## Related Value Streams

- DevTools 单请求创建（捕获 → 选请求 → 填表 → createTask）。本增量增强「选请求」步骤。
- `2026-08-27-task-chrome-plugin-*`：自动运行/镜像，正交。

Greenfield 于「用户可控排序 + 类型过滤」；过滤下拉为存量增强。

## 端到端

打开 DevTools 面板 → Network 产生请求 → 用搜索/方法/状态/类型缩小列表 → 点表头排序 → 点选一条 → 创建任务（既有）。

## 增量

| ID | 切片 | 用户可见价值 | 测试点 |
|----|------|--------------|--------|
| Inc-1 | 过滤 + 列排序 | 能从 200 条缓冲中按类型/方法/状态/文本筛，并按列排序后点选 | T1–T14 见测试意图 |

无第二增量：不改 Popup、不改 SW 捕获。

## YAML

不写入根 `value-stream.yaml`（无后端字段变更）。
