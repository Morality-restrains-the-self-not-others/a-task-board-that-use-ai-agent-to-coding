# NFR 澄清: 修复 git-service 启动 mkdir 权限拒绝

> 输入:
> - 设计文档: `.claude/skills/1-brainstorming-设计文档/design.md`
> - 价值流文档: `docs/superpowers/plans/2026-06-30-git-service-mkdir-permission-fix-value-stream.md`
>
> 输出使用者: `/6-ddd-领域设计驱动`, `/7-plans-实施计划`, `/8-build-构建`

## NFR 概览表

| 类别 | 等级 | 理由 |
|------|------|------|
| 性能 | L0 | 不适用 — 启动路径变更，无运行时性能影响 |
| 可伸缩性 | L0 | 不适用 — 宿主机目录路径变更，无横向扩展影响 |
| 可用性 | L0 | 不适用 — 修复使服务可启动，但不改变可用性架构 |
| 安全性 | L0 | 不适用 — 路径变更，无权限模型或认证流程修改 |
| 数据一致性 | L0 | 不适用 — 无数据库或状态变更 |
| 容错机制 | L0 | 不适用 — 无重试/超时/断路器逻辑变更 |
| 可观测性 | L0 | 不适用 — 无日志/指标/追踪变更 |
| 合规与隐私 | L0 | 不适用 — 无数据处理变更 |
| 可维护性 | L0 | 不适用 — 无 API/配置格式变更 |

## 跳过声明

本变更为基础设施 bug 修复：`gitService/run.sh` 中 3 行路径调整。

**跳过全部 NFR 类别的理由：**
1. 设计文档明确声明「无特殊 NFR 要求」
2. 增量不引入新的数据流或外部依赖
3. 仅变更宿主机脚本中的目录路径（`./gitlab_home/bootstrap_marks` → `./.bootstrap_marks`）
4. 无新增端点、数据库表、或用户可见功能

## 领域模型影响

无。本变更不引入领域概念，不影响领域模型。

## 质量场景

无。无可验证的质量属性场景。
