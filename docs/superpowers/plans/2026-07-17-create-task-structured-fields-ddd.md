# DDD 备注：创建任务结构化可选字段

## 限界上下文

`taskTaskService` 任务聚合不变；`description` 仍为任务内容值对象（TEXT）。

## 领域事件

无新增意图/事件。创建任务既有事件（若平台已投递）契约不变。

## 模型

前端引入 UI 辅助值对象「结构化任务说明」七字段，**不**提升为持久化领域实体；持久化边界仍是 `Task.description`。
