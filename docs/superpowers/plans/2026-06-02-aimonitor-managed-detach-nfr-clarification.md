# NFR 澄清：AiMonitor managed detach 修复

**日期**: 2026-06-02  
**默认级别**: L2（Standard）

## 适用类别

| 类别 | 级别 | 场景 |
|------|------|------|
| 可用性 | L2 | runAll 启动 ai-monitor 应在 180s 内 healthy |
| 可靠性 | L2 | SSH 启动命令必须返回，容器后台持续运行 |
| 可维护性 | L2 | 与 redis/kafka/git-service managed 模式一致 |
| 性能 | L1 | 无新性能目标；启动时间从「永不返回」→ ~2s |
| 安全 | — | 无变更 |
| 可扩展性 | — | 无变更 |

## 跳过类别

数据一致性、多租户、审计 — 不涉及。

## 对领域模型影响

**无** — 基础设施 Shell 脚本，不进入 DDD 建模。
