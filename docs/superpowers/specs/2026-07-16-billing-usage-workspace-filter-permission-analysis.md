# 权限分析：用量页工作空间筛选

## 结论

无新权限面。复用既有：

| API | 既有鉴权 |
|-----|----------|
| `GET .../billing/usages/?workspace_id=` | 租户计费读（与 project_id 同级） |
| `GET .../workspaces/` | 租户工作空间列表（下拉） |

筛选仅收窄结果集，不扩大数据可见范围。
