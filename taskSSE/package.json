{
  "name": "task-sse",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dedicated SSE sidecar for task-detail server-startup-status (Redis/Kafka transport)",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test test/*.test.mjs src/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "kafkajs": "^2.2.4",
    "redis": "^4.7.0"
  }
}
