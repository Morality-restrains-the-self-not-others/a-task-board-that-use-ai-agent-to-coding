

<!-- 归档索引：非当日的条目已按天归档至 archive/completed/ -->
<!-- 2026-09-02: 22 条 → [./archive/completed/OPT_COMPLETED_2026-09-02.md](./archive/completed/OPT_COMPLETED_2026-09-02.md) -->
<!-- 2026-09-01: 14 条 → [./archive/completed/OPT_COMPLETED_2026-09-01.md](./archive/completed/OPT_COMPLETED_2026-09-01.md) -->
<!-- 2026-08-31: 40 条 → [./archive/completed/OPT_COMPLETED_2026-08-31.md](./archive/completed/OPT_COMPLETED_2026-08-31.md) -->
<!-- 2026-08-30: 19 条 → [./archive/completed/OPT_COMPLETED_2026-08-30.md](./archive/completed/OPT_COMPLETED_2026-08-30.md) -->
<!-- 2026-08-29: 36 条 → [./archive/completed/OPT_COMPLETED_2026-08-29.md](./archive/completed/OPT_COMPLETED_2026-08-29.md) -->
<!-- 2026-08-28: 71 条 → [./archive/completed/OPT_COMPLETED_2026-08-28.md](./archive/completed/OPT_COMPLETED_2026-08-28.md) -->
<!-- 2026-08-27: 29 条 → [./archive/completed/OPT_COMPLETED_2026-08-27.md](./archive/completed/OPT_COMPLETED_2026-08-27.md) -->
<!-- 2026-08-26: 21 条 → [./archive/completed/OPT_COMPLETED_2026-08-26.md](./archive/completed/OPT_COMPLETED_2026-08-26.md) -->
<!-- 2026-08-25: 34 条 → [./archive/completed/OPT_COMPLETED_2026-08-25.md](./archive/completed/OPT_COMPLETED_2026-08-25.md) -->
<!-- 2026-08-24: 51 条 → [./archive/completed/OPT_COMPLETED_2026-08-24.md](./archive/completed/OPT_COMPLETED_2026-08-24.md) -->
<!-- 2026-08-23: 46 条 → [./archive/completed/OPT_COMPLETED_2026-08-23.md](./archive/completed/OPT_COMPLETED_2026-08-23.md) -->
<!-- 2026-08-22: 41 条 → [./archive/completed/OPT_COMPLETED_2026-08-22.md](./archive/completed/OPT_COMPLETED_2026-08-22.md) -->
<!-- 2026-08-21: 35 条 → [./archive/completed/OPT_COMPLETED_2026-08-21.md](./archive/completed/OPT_COMPLETED_2026-08-21.md) -->
<!-- 2026-08-20: 60 条 → [./archive/completed/OPT_COMPLETED_2026-08-20.md](./archive/completed/OPT_COMPLETED_2026-08-20.md) -->
<!-- 2026-08-19: 39 条 → [./archive/completed/OPT_COMPLETED_2026-08-19.md](./archive/completed/OPT_COMPLETED_2026-08-19.md) -->
<!-- 2026-08-18: 12 条 → [./archive/completed/OPT_COMPLETED_2026-08-18.md](./archive/completed/OPT_COMPLETED_2026-08-18.md) -->
<!-- 2026-08-17: 35 条 → [./archive/completed/OPT_COMPLETED_2026-08-17.md](./archive/completed/OPT_COMPLETED_2026-08-17.md) -->
<!-- 2026-08-16: 34 条 → [./archive/completed/OPT_COMPLETED_2026-08-16.md](./archive/completed/OPT_COMPLETED_2026-08-16.md) -->
<!-- 2026-08-15: 21 条 → [./archive/completed/OPT_COMPLETED_2026-08-15.md](./archive/completed/OPT_COMPLETED_2026-08-15.md) -->
<!-- 2026-08-14: 53 条 → [./archive/completed/OPT_COMPLETED_2026-08-14.md](./archive/completed/OPT_COMPLETED_2026-08-14.md) -->
<!-- 2026-08-13: 51 条 → [./archive/completed/OPT_COMPLETED_2026-08-13.md](./archive/completed/OPT_COMPLETED_2026-08-13.md) -->
<!-- 2026-08-12: 40 条 → [./archive/completed/OPT_COMPLETED_2026-08-12.md](./archive/completed/OPT_COMPLETED_2026-08-12.md) -->
<!-- 2026-08-11: 57 条 → [./archive/completed/OPT_COMPLETED_2026-08-11.md](./archive/completed/OPT_COMPLETED_2026-08-11.md) -->
<!-- 2026-08-10: 36 条 → [./archive/completed/OPT_COMPLETED_2026-08-10.md](./archive/completed/OPT_COMPLETED_2026-08-10.md) -->
<!-- 2026-08-09: 3 条 → [./archive/completed/OPT_COMPLETED_2026-08-09.md](./archive/completed/OPT_COMPLETED_2026-08-09.md) -->
<!-- 2026-08-07: 16 条 → [./archive/completed/OPT_COMPLETED_2026-08-07.md](./archive/completed/OPT_COMPLETED_2026-08-07.md) -->
<!-- 2026-08-06: 5 条 → [./archive/completed/OPT_COMPLETED_2026-08-06.md](./archive/completed/OPT_COMPLETED_2026-08-06.md) -->
<!-- 2026-07-29: 3 条 → [./archive/completed/OPT_COMPLETED_2026-07-29.md](./archive/completed/OPT_COMPLETED_2026-07-29.md) -->
<!-- 2026-07-28: 11 条 → [./archive/completed/OPT_COMPLETED_2026-07-28.md](./archive/completed/OPT_COMPLETED_2026-07-28.md) -->
<!-- 2026-07-27: 29 条 → [./archive/completed/OPT_COMPLETED_2026-07-27.md](./archive/completed/OPT_COMPLETED_2026-07-27.md) -->
<!-- 2026-07-26: 31 条 → [./archive/completed/OPT_COMPLETED_2026-07-26.md](./archive/completed/OPT_COMPLETED_2026-07-26.md) -->
<!-- 2026-07-25: 101 条 → [./archive/completed/OPT_COMPLETED_2026-07-25.md](./archive/completed/OPT_COMPLETED_2026-07-25.md) -->
<!-- 2026-07-24: 68 条 → [./archive/completed/OPT_COMPLETED_2026-07-24.md](./archive/completed/OPT_COMPLETED_2026-07-24.md) -->
<!-- 2026-07-23: 74 条 → [./archive/completed/OPT_COMPLETED_2026-07-23.md](./archive/completed/OPT_COMPLETED_2026-07-23.md) -->
<!-- 2026-07-22: 32 条 → [./archive/completed/OPT_COMPLETED_2026-07-22.md](./archive/completed/OPT_COMPLETED_2026-07-22.md) -->
<!-- 2026-07-21: 10 条 → [./archive/completed/OPT_COMPLETED_2026-07-21.md](./archive/completed/OPT_COMPLETED_2026-07-21.md) -->
<!-- 2026-07-20: 22 条 → [./archive/completed/OPT_COMPLETED_2026-07-20.md](./archive/completed/OPT_COMPLETED_2026-07-20.md) -->
<!-- 2026-07-19: 26 条 → [./archive/completed/OPT_COMPLETED_2026-07-19.md](./archive/completed/OPT_COMPLETED_2026-07-19.md) -->
<!-- 2026-07-18: 106 条 → [./archive/completed/OPT_COMPLETED_2026-07-18.md](./archive/completed/OPT_COMPLETED_2026-07-18.md) -->
<!-- 2026-07-17: 6 条 → [./archive/completed/OPT_COMPLETED_2026-07-17.md](./archive/completed/OPT_COMPLETED_2026-07-17.md) -->

## [OPT-20260904-011] completed

**Logged**: 2026-09-04
**Priority**: medium
**Status**: completed
**Completed**: 2026-09-04
**Completion-Note**: 以 v125 `.full` 为基，用 `docs/architecture/scripts/backfill_full_archimate_views.py` 按版本序合并各版 slice 元素/关系并累积 Views；enterprise v132=18 视图、application-integration v132=23 视图。Archi `--loadModel` 全绿；`check_archimate_full_inherits_views.py --strict-debt` 全绿；`DEBT_VERSIONS` 已清空。

## [OPT-20260902-017] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: register-precise-restart 提示改 192.168.1.10:9999
**Area**: infra

### Summary
`scripts/register-precise-restart.sh` 成功提示改为当前 clone-run 9999 地址（`http://192.168.1.10:9999/`），不要再写 `10.2.150.68`。

### Details
本机部署 9999 在 `192.168.1.10:9999`（`~/bin/daydaymoney-deploy`）。登记脚本仍 echo `http://10.2.150.68:9999/`。约束 42 已改地址。验收：`rg -n '10\\.2\\.150\\.68:9999' scripts/register-precise-restart.sh` 无命中；`rg -n '192\\.168\\.1\\.10:9999' scripts/register-precise-restart.sh` 有命中。

### Metadata
- Source: goal-overview
- Related Files: scripts/register-precise-restart.sh, .ai/01_project_constraints/42_precise_restart_service_registration.md
- Tags: precise-restart, clone-run, 9999

---

## [OPT-20260902-019] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: 空登记改 info toast 不走 showRequestError
**Area**: frontend

### Summary
空登记提示改用 info toast，不要走红色 `showRequestError` 横幅。

### Details
`POST /api/precise-restart` 空登记已是 200 `{status:empty}`，但 UI 仍用 `showRequestError` 展示「没有已登记的服务」，看起来像失败。应在 `showEmptyPreciseRestartRegistry` 改 `showToast(..., {type:'info'})` 或独立 info banner，且不设 `data-traceId`（非请求失败）。验收：`node --test runAll/src/status_ui/js/precise_restart_empty_registry.test.js` 断言 helper 不再调用 `showRequestError`；`rg -n "showEmptyPreciseRestartRegistry" -A8 runAll/src/status_ui/js/05.js` 含 `showToast`。

### Metadata
- Source: goal-overview
- Related Files: runAll/src/status_ui/js/05.js, runAll/src/status_ui/js/11.js, runAll/src/status_ui/js/17.js
- Tags: precise-restart, ux, 9999

---

## [OPT-20260902-028] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: oauth_gitsite 只给 OAuth 能力站点盖章
**Area**: frontend

### Summary
`stampRepoIdentityOauthGitsite` 只给 OAuth 能力站点（GitHub / GitLab）写 `oauth_gitsite`，不要给 generic `git.example` 打 L2。

### Details
当前按 URL host 一律盖章，单测期望 `git.example` 也带 `oauth_gitsite`。taskTaskService `stampImplicitCommentOAuthGitsite` 已用 `isOAuthCapableGitsite` 过滤。FE 对齐后可避免 credential `FetchAccessToken` 打未知 host。验收：`npx vitest run src/utils/createTaskGitIdentityGate.test.js` 中 generic git 无 `oauth_gitsite`，GitLab ram-work 仍有。

### Metadata
- Source: goal-overview
- Related Files: taskFE/app/src/utils/createTaskGitIdentityGate.js, taskTaskService/src/comment_oauth_grant.go
- Tags: git-oauth, comment-l2

---

## [OPT-20260902-001] completed

**Logged**: 2026-09-02
**Priority**: medium
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: taskSSE 只读本目录 gatewayInternalSecret
**Area**: backend

### Summary
taskSSE 运行时仍直读 `conf/gateway/task-gateway` 的 `gatewayInternalSecret`；改为 `sync.manifest` 抽到本目录片段后只读 `gateway/task-sse`。

### Details
本次 403 修复让 `findRepoRoot` 尊重 CONF_ROOT，但仍 `loadConfRead('taskGateway')` / `loadYamlApp(..., 'gateway/task-gateway')`，违反约束 29。应 pick `gatewayInternalSecret` 到 `conf/gateway/task-sse/` GENERATED 片段，运行时只 overlay `conf-local/gateway/task-sse/`（或同源 fragment）。验收：`rg "taskGateway|gateway/task-gateway" taskSSE/src/config.mjs` 无跨 app 读取；`node --test src/config.root.test.mjs` 仍从本目录 overlay 读到密钥。

### Metadata
- Source: goal-overview
- Related Files: taskSSE/src/config.mjs, conf/gateway/task-sse/sync.manifest.yaml
- Tags: conf, constraint-29, task-sse

---

## [OPT-20260902-021] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: WorkPanelTaskSearch 根节点自带定宽
**Area**: frontend

### Summary
把 `WorkPanelTaskSearch` 根节点从 `flex-1` 改为由父级定宽，去掉工作面板工具栏上的固定宽度包裹层。

### Details
合并顶栏后搜索根节点 `flex-1 min-w-[12rem]` 会在 shrink-wrap 的标题区内把整行撑开，因此 `WorkPanelHeader` 用 `w-64` 包裹。后续应让搜索组件根 class 为 `w-full min-w-[12rem]`（或接受 `class` prop），父级直接定宽。验收：`rg "flex-1 min-w-\\[12rem\\]" taskFE/app/src/components/WorkPanelTaskSearch.vue` 无命中；`npx vitest run src/views/WorkPanelHeader.legend.test.js` 全绿。

### Metadata
- Source: goal-overview
- Related Files: taskFE/app/src/components/WorkPanelTaskSearch.vue, taskFE/app/src/views/WorkPanelHeader.vue
- Tags: work-panel, layout, flex

---

## [OPT-20260902-006] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: OAuth 回流 repo_url 写回首行
**Area**: frontend

### Summary
创建项目 OAuth 回流后把 `repo_url` 写回第一行 Git 仓库输入框。

### Details
当前只把 `grant_ticket` 记入 sessionStorage；整页跳转后表单仓库 URL 可能被清空，用户需再粘贴。bootstrap 若 `route.query.repo_url` 非空且首行空白，应填入并触发校验。验收：`npx vitest run src/composables/useCreateProjectGitRepoRows.batch.test.js` 断言 bootstrap 后 `gitRepoRows[0].url` 等于 query `repo_url`。

### Metadata
- Source: goal-overview
- Related Files: taskFE/app/src/composables/useCreateProjectGitRepoRows.js
- Tags: oauth, create-project, grant-ticket

---

## [OPT-20260902-008] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: precise-compile.sh --install
**Area**: infra

### Summary
`precise-compile.sh` 成功后可选把 `deploy-binaries/` 装进 `$DEPLOY_ROOT`（`install-local-artifacts.sh`）。

### Details
当前脚本只写源码仓 `deploy-binaries/`，clone-run 树 `$HOME/bin/daydaymoney-deploy` 不会自动换 ELF。增加 `--install`（默认关）调用 `runAll/scripts/install-local-artifacts.sh "$DEST" "${DEPLOY_ROOT:-$HOME/bin/daydaymoney-deploy}"`。验收：`python3 -m pytest runAll/scripts/tests/test_precise_compile.py -q` 增加假 dest 装入假 deploy root 的断言。

### Metadata
- Source: goal-overview
- Related Files: runAll/scripts/precise-compile.sh, runAll/scripts/install-local-artifacts.sh
- Tags: deploy-binaries, precise-compile, clone-run

---

## [OPT-20260902-022] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: 非 completed 步骤 command 展示在折叠区外
**Area**: frontend

### Summary
非 completed 步骤若已有 `tool_calls.arguments.command`，把命令也展示在折叠区外的 summary。

### Details
`agentStepCardTitle` 仅在 `state === 'completed'` 把 command 写入标题；`agentStepPlainBodyForPre` 只要 `agentStepTitleCommands` 非空就清空 indigo pre。因此 `calling_tool` / `thinking` 期间命令既不在标题也不在 pre。验收：新增 vitest 覆盖 calling_tool + command 时 summary 可见命令；`npx vitest run src/utils/taskDetailExecLogFormatters.agentStepTitle.test.js src/components/task-detail/TaskDetailAgentStepCard.commandOutsideFold.test.js` 全绿。

### Metadata
- Source: goal-overview
- Related Files: taskFE/app/src/utils/taskDetailExecLogFormatters.js, taskFE/app/src/components/task-detail/TaskDetailAgentStepCardHeader.vue
- Tags: agent-steps, command-preview, calling_tool

---

## [OPT-20260902-013] completed

**Logged**: 2026-09-02
**Priority**: medium
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: 连接页区域下拉分组+pending_node 提示
**Area**: frontend

### Summary
工作区 GitLab 连接页区域下拉复用 `groupGitlabRegionsByProvider`，对 `pending_node` 显示人工开通提示。

### Details
OrderCreate 已按云厂商 optgroup 并提示 pending_node；`WorkspaceSettingsGitlabConnection.vue` 仍是扁平 option。应 (1) 引入同一 util；(2) 选中 pending_node 时提示去购买/等待开通而不是空 web_url。验收：补合同测，购买页与连接页分组文案一致。

### Metadata
- Source: goal-overview
- Related Files: taskFE/app/src/views/WorkspaceSettingsGitlabConnection.vue, taskFE/app/src/utils/gitlabRegionSelect.js
- Tags: gitlab, frontend, pending_node

---

## [OPT-20260903-001] completed

**Logged**: 2026-09-03
**Priority**: medium
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: taskFE 94d969a 已实现：connectOverCDP PASS 后 process.exit(0)，wait-only 模式只盯 PLAYWRIGHT_WAIT_TASK_ID；node --check 通过
**Area**: testing

### Summary
Playwright `connectOverCDP` 成功后必须 `process.exit(0)`；Fork 验收只能认本次 API 返回的 task id，禁止匹配任意已打开的 task-detail 标签页。

### Details
CDP 连接会挂住 Node 事件循环，只 `return` 的 runner 在 PASS 后仍不退出。多标签时 `isForkedTaskUrl` 曾命中上一轮 Fork 页导致假超时。验收：`PLAYWRIGHT_WAIT_TASK_ID` 只盯该 id；PASS 日志后进程 2s 内退出码 0。

### Metadata
- Source: goal-overview
- Related Files: taskFE/tests/TaskDetail.fork-auto-run-push-pr.cdp.mjs
- Tags: playwright, cdp, fork-e2e

---

## [OPT-20260902-009] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: collect-deploy-binaries-on-commit.sh 导出 COLLECT_SKIP_SHA=1（runAll 9e81612 测试 + meta d4a3a152）；错 pin 时 hook exit 0 且无 SHA MISMATCH；pytest 6 passed
**Area**: infra

### Summary
提交钩子归集 `deploy-binaries` 时使用 `COLLECT_SKIP_SHA=1`，避免与 GitHub pin 误报 SHA MISMATCH。

### Details
源码机 ELF 与 `conf.example/releases.yaml` 的 Release pin 本就不同；`collect-deploy-binaries-on-commit.sh` 仍跑校验并 fail-open 刷屏。应在钩子里导出 `COLLECT_SKIP_SHA=1`。验收：`python3 -m pytest runAll/scripts/tests/test_collect_on_commit.py -q` 断言钩子环境含 skip，且错 pin 时仍 exit 0 且无 SHA MISMATCH。

### Metadata
- Source: goal-overview
- Related Files: scripts/lib/collect-deploy-binaries-on-commit.sh, runAll/scripts/collect-deploy-binaries.sh
- Tags: deploy-binaries, sha, pre-commit

---

## [OPT-20260902-012] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: runAll 28b3bbf：rsyncConfLocal 先写 conf-local.incoming staging 再 swapConfLocalDir rename 交换；假 rsync 失败注入测例验证 dest 树不变，go test 全绿
**Area**: backend

### Summary
把部署 9999 成功路径的 `conf-local` rsync 改成临时目录 + `rename` 交换，避免半写入。

### Details
当前 `rsync -a --delete` 直接打 `$DEPLOY_ROOT/conf-local/`，中断时可能半更新。可先 rsync 到 `conf-local.incoming.$$` 再 `mv` 替换（或 rsync `--delay-updates`）。验收：`go test ./runAll/src -run RsyncConfLocal` 仍绿；另加测例：rsync 中途失败时 dest 树与失败前一致（可用 `rsyncConfLocal` 注入假 rsync）。

### Metadata
- Source: goal-overview
- Related Files: runAll/src/source_compile_deploy.go
- Tags: deploy, conf-local, rsync

---

## [OPT-20260902-016] completed

**Logged**: 2026-09-02
**Priority**: medium
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: register-precise-restart.sh 默认写 name/state/unix_ts 三列并原地升级旧纯名字行（meta 36f91837）；runAll dc06db2 将无时间戳登记视为过期、重登记补戳；registry/expired 测试绿
**Area**: infra

### Summary
`scripts/register-precise-restart.sh` 写入带 unix 时间戳的登记行，使 24h TTL 能淘汰无时间戳的积压名单。

### Details
clone-run 9999 修好 SOURCE_ROOT 后，源码仓 `.runall/precise_restart_services.txt` 有 73 条纯服务名（无 `\tstate\tunix_ts`），`registrationExpired` 视为未过期。点「精准编译重启」会按依赖序编译整份名单。应让登记脚本默认写新格式；可选 `--clear` 后只登记本会话改动的服务。验收：`register-precise-restart.sh task-bill` 写出三列；纯名字行在 TTL 扫描中可配置为过期或迁移时补戳。

### Metadata
- Source: goal-overview
- Related Files: scripts/register-precise-restart.sh, runAll/src/precise_restart_registry.go
- Tags: precise-restart, ttl, clone-run

---

## [OPT-20260902-024] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: docs d281fcb：DESIGN.md Stitch 风格 preview.html + preview-dark.html（含 #7241F2 色板/字阶/按钮/卡片）；db test_check_design_md.py 6 passed
**Area**: frontend

### Summary
为仓库根 `DESIGN.md` 生成 Stitch 风格 `preview.html` / `preview-dark.html`（色板、字阶、按钮、卡片），与 awesome-design-md 目录结构对齐。

### Details
当前只落地 markdown SSOT + CI 令牌锁。catalog 站点另有 HTML 预览便于 agent 对照。可从 YAML `colors` / 组件段渲染静态 preview，放 `docs/design/`（不要覆盖产品 SPA）。验收：`test -f docs/design/preview.html`；HTML 含 `#7241F2`；`python3 db/scripts/ci/test_check_design_md.py` 仍全绿。

### Metadata
- Source: goal-overview
- Related Files: DESIGN.md, .claude/skills/design-md/references/format.md
- Tags: design-md, preview, stitch

---

## [OPT-20260902-011] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: 评论 Git OAuth check_failed 徽标支持点击重试（force 重探测，仍不弹窗）；taskFE 501d9db，vitest 3 套件绿
**Area**: frontend

### Summary
评论区 Git OAuth `check_failed` 徽标增加用户点击「重试」探测（非轮询）。

### Details
超时后同一 URL 记入 `probedUrls` 不再自动重试。可在 `CommentExecutionGitOauthBadge` 对 `kind=check_failed` 加用户触发重试：移出 `probedUrls` 再跑一次 probe。验收：`CommentExecutionGitOauthBadge.test.js` 断言重试控件为 button（或带 clickGuard）；`useCommentGitOauthAccessTokenProbe.test.js` 断言点击后再次调用 probe 且仍不弹窗。

### Metadata
- Source: goal-overview
- Related Files: taskFE/app/src/components/task-detail/CommentExecutionGitOauthBadge.vue, taskFE/app/src/composables/taskDetail/useCommentGitOauthAccessTokenProbe.js
- Tags: git-oauth, badge, retry

---

## [OPT-20260902-025] completed

**Logged**: 2026-09-02
**Priority**: medium
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: grant_ticket 消费语义：已消费 ticket 不再作为 Fork 门禁已绑定证据，回落项目 L2；taskFE a85dddc
**Area**: frontend

### Summary
Fork 自动运行 OAuth 门禁不要把已消费的 session `grant_ticket` 显示成「已绑定」。

### Details
源任务创建时 ticket 已被 consume，sessionStorage 仍留 ticket id，Fork 弹窗 `hasSessionGrantForRepo` 仍显示 `fork-auto-run-oauth-bound`。后端种下已能从源评论 L2 补上，但 UI 与真实可消费 ticket 不一致。验收：已消费 ticket 不再单独放行；同用户源评论 L2 或项目 L2 才显示已绑定。

### Metadata
- Source: goal-overview
- Related Files: taskFE/app/src/utils/grantTicketSession.js, taskFE/app/src/composables/useCreateTaskRepoOAuth.js, taskFE/app/src/utils/createTaskOauthGate.js
- Tags: git-oauth, fork, grant-ticket

---

## [OPT-20260902-010] completed

**Logged**: 2026-09-02
**Priority**: medium
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: GitHub issue token 探测 ≤6s 显式超时快路径；taskGitOauth 6e50613，go test 全绿
**Area**: backend

### Summary
缩短 GitHub AccessToken probe（`issue_failed`）耗时，避免前端 6s abort 后上游仍跑到 ~15s。

### Details
traceId `e0a7ec3d-133a-4ff9-aa9d-15780e06d1eb`：gateway `git-oauth-token` 499 @5972ms，taskGitOauth `/api/git-oauth/user-app-connection/` 200 @14826ms，`git_oauth_access_token_probe valid=false reason=issue_failed`。2026-09-02 再发 `7fa891f7-4d25-4e87-989c-47794f21b5a8`：同一 `issue_failed` 路径 `duration_ms=100710`。前端超时已改为评论区徽标，但后端仍浪费 15s～100s 探测。应给 GitHub issue token 设更短超时或失败快路径。验收：对 `issue_failed` 路径加测，断言探测在 ≤6s 返回（或显式短超时），`go test` 覆盖该分支。

### Metadata
- Source: goal-overview
- Related Files: taskGitOauth
- Tags: git-oauth, probe-timeout, github

---

## [OPT-20260902-002] completed

**Logged**: 2026-09-02
**Priority**: medium
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: Promtail 已挂载真实日志根 /home/ljy/bin/daydaymoney-deploy/logs（此前被 runAll TestAPIObservabilityClearAll 测试劫持到空 temp 目录）；Loki label/job/values 含 task-sse，trace 查询 streams≥1
**Area**: infra

### Summary
对齐 Promtail `task-sse` 采集路径与 clone-run 实际日志，使 Loki `{job=~".+"} |= "<trace>"` 能命中。

### Details
trace `cb9b0ffb7d6bdc9ce9fd2a424a8fff95` 时 Loki ready 但 `label/job/values` 空、query 0 bytes。Promtail 配置 `__path__: /var/log/runall/task-sse.log`，实际文件在 `/home/ljy/bin/daydaymoney-deploy/logs/task-sse.log`。验收：`curl Loki label job values` 含 `task-sse`；同一 trace 再查 `{job=~".+"} |= "<id>"` streams≥1。

### Metadata
- Source: goal-overview
- Related Files: AiMonitor/promtail/promtail-local.yaml, daydaymoney-deploy/logs/task-sse.log
- Tags: loki, promtail, task-sse, observability

---

## [OPT-20260902-015] completed

**Logged**: 2026-09-02
**Priority**: medium
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: 同上修复后 Loki 含 task-project-service job，trace 查询 streams≥1；config 本就含该 job（/var/log/runall/task-project-service.log）
**Area**: infra

### Summary
把 `task-project-service` 的 clone-run 日志纳入 Promtail，使 Loki 能按 `trace_id` 检索标题翻译失败。

### Details
trace `fb1f8148-d520-41ad-9497-efc9ced42bf4` 在 Loki `{job=~".+"}` 7 天 0 条；job 列表无 `task-project-service`。进程 fd 写入 `/home/ljy/bin/daydaymoney-deploy/logs/task-project-service.log`，Promtail `__path__` 为 `/var/log/runall/task-project-service.log`。与 OPT-20260902-002（task-sse）同类。验收：`curl -sS http://10.2.150.68:3100/loki/api/v1/label/job/values` 含 `task-project-service`；对一条新 translate 502 的 `trace_id` 执行 `{job=~".+"} |= "<id>"` streams≥1。

### Metadata
- Source: goal-overview
- Related Files: AiMonitor/promtail/promtail-local.yaml, runAll/scripts/generate-promtail-config.sh
- Tags: loki, promtail, task-project-service, observability

---

## [OPT-20260902-018] completed

**Logged**: 2026-09-02
**Priority**: medium
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: 同上修复后 Loki 含 task-git-oauth 与 task-auth job，trace 查询 streams≥1
**Area**: infra

### Summary
让 Promtail 采集 `daydaymoney-deploy/logs/task-git-oauth.log` 与 `task-auth.log`，使 Loki `{job=~".+"} |= "<trace>"` 能命中 OAuth 探测链路。

### Details
trace `7fa891f7-4d25-4e87-989c-47794f21b5a8`：Loki ready，但 7 天 `{job=~".+"}` 与 `|= "<id>"` 均为 0 streams；同机文件 `/home/ljy/bin/daydaymoney-deploy/logs/task-git-oauth.log` 有 `git_oauth_access_token_probe` + `duration_ms=100710`。与 OPT-20260902-002（task-sse 路径）同类：采集 path 未覆盖 clone-run 日志根。验收：`curl -s "$LOKI/loki/api/v1/query_range"` `{job=~".+"} |= "7fa891f7"` 或新产生的 git-oauth `http_request` streams≥1。

### Metadata
- Source: goal-overview
- Related Files: AiMonitor/promtail/promtail-local.yaml, daydaymoney-deploy/logs/task-git-oauth.log
- Tags: loki, promtail, git-oauth, observability

---

## [OPT-20260903-003] completed

**Logged**: 2026-09-03
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: runAll 内联 OTel-free 金丝雀监听 helper（X-RunAll-Pid + SO_REUSEPORT ListenConfig）到 src/ui_reuseport_listen*.go，删除 tracelog require/replace。go list -m all 不再含 go.opentelemetry.io/otel；go test ./src -run Canary|AllowOverlap|StartUIServer_Bind 绿。tracelog 本体未动，16 个经 ListenAndServe 消费者零改动。
**Area**: infra

### Summary
把 `tracelog.ListenAndServe` / `ReusePortListenConfig` 拆到无 OTel 的小包，避免 runAll 仅为金丝雀监听拉入 otel 间接依赖。

### Details
ADR-0058 后 `runAll/go.mod` 通过 `replace tracelog` 引入 `go.opentelemetry.io/*`。`listen.go` 不使用 tracing。验收：`go list -m all` 在 `runAll` 中不再出现 `go.opentelemetry.io/otel`，且 `go test ./src -run 'Canary|AllowOverlap|StartUIServer_Bind'` 仍绿。

### Metadata
- Source: goal-overview
- Related Files: shareLib/tracelog/listen.go, runAll/go.mod, runAll/src/ui_server.go
- Tags: tracelog, reuseport, go-mod, canary-restart

---

## [OPT-20260903-004] completed

**Logged**: 2026-09-03
**Priority**: high
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: runAll 单测劫持共享 aimonitor-promtail 已根治：(1) reloadPromtailForTraceShipping 对 Go t.TempDir 日志根直接 skipped（promtailLogRoot/isGoTestTempRoot + reloadPromtailShellRun 可测 seam）；(2) ClearAllObservability 对非生产日志根跳过真实 reset 脚本（resetter=nil），不再停重建共享 Loki/Tempo/Prometheus。回归单测 TestReloadPromtail_SkipsSharedContainerOnGoTestTempRoot / TestPromtailLogRoot_FlagsGoTestTempRoot。实测 go test ./src -run Observability|TraceShipping 后 docker inspect aimonitor-promtail mount 仍为 /home/ljy/bin/daydaymoney-deploy/logs（此前被 /tmp/Test* 劫持，本会话当场复现并已恢复）。
**Area**: infra

### Summary
runAll 测试/启动把共享 `aimonitor-promtail` 容器劫持到 t.TempDir，导致 Loki 空采；trace shipping 起 promtail 须隔离或门禁。

### Details
`runAll/src/trace_shipping.go` VerifyTraceShipping/启动路径对固定名 `aimonitor-promtail` 执行 `docker compose up -d promtail`，RUNALL_LOG_ROOT 取自 Runner FileRoot。runAll 测试（如 `TestAPIObservabilityClearAll`、`trace_shipping_test.go`）用 `t.TempDir()` 作 FileRoot 构造 NewRunner 即触发，把共享 promtail 指到空 temp 目录（`/tmp/TestAPIObservabilityClearAll*/001`），测试结束容器残留（restart:unless-stopped）→ Loki 全空。OPT-20260902-002/015/018 的「Loki 0 bytes」根因即此。修复方向：非生产 FileRoot（/tmp/Test*/、t.TempDir）时禁止真实 compose up（用 isolated 容器名或 mock）；ClearAll 后恢复 promtail 到真实日志根。验收：跑一遍 runAll `go test ./src -run 'Observability|TraceShipping'` 后 `docker inspect aimonitor-promtail` 的 /var/log/runall mount 仍指向生产日志根（/home/ljy/bin/daydaymoney-deploy/logs）。

### Metadata
- Source: night-opt-20260903
- Related Files: runAll/src/trace_shipping.go, runAll/src/ui_test.go, runAll/src/trace_shipping_test.go, AiMonitor/docker-compose.yaml
- Tags: promtail, loki, test-isolation, observability, precise-restart

---

## [OPT-20260902-026] completed

**Logged**: 2026-09-02
**Priority**: high
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: runAll 对带病存活（alive-but-unhealthy）进程 fail-fast 自动重启，不等 5 分钟 cron watchdog；注册保留已有语义；新增 3 个回归测试（存活触发/上限/无句柄跳过）
**Area**: infra

### Summary
精准编译重启后须保证 `task-cloud-service` :8018 在健康检查成功前不可清空登记；失败则保留登记并立即拉起，不要等到 5 分钟 watchdog。

### Details
trace `0a7ca81c-2082-4dcb-a34b-f61bbe14c85e`：`.runall/precise_restart_consumed_at=1788346066`（18:47）后 :8018 无监听，APISIX 对 feature-params 三次 connection-refused 均 0ms 后 502。watchdog 18:50:01 才 `unhealthy … no listener, restarting`，18:50:05 恢复。OPT-20260827-039 的 APISIX retries:2 无法覆盖分钟级空窗。验收：精准重启失败时 8018 仍在登记文件；健康检查失败 30s 内由 runAll 再 start，而不是等 cron。

### Metadata
- Source: goal-overview
- Related Files: runAll/src/precise_restart.go, runAll/src/runner.go, conf/runAll.yaml
- Tags: precise-restart, task-cloud-service, watchdog, 502

---

## [OPT-20260903-002] completed

**Logged**: 2026-09-03
**Priority**: medium
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: taskFE：层图快照补写 git_pr 子评论后，若评论 Feed 缺失该 PR URL 则补拉一次 comments（同 task+URL 仅一次）；新增 layerGraphPrBackfillRefetch 纯函数与回归测试
**Area**: frontend

### Summary
层图 GET 在返回快照时补写 git_pr 子评论后，任务详情应再拉一次 comments，避免与并行 comments 请求竞态导致 Feed 暂时没有回帖。

### Details
`ensureGitPrRepliesFromGraphDoc` 在 `container-layer-graph` GET 内同步创建 `cmt_*` git_pr 子评论。前端同时请求 comments 与 layer-graph 时，comments 可能早于补写完成。验收：layer-graph 响应含 `git_remote.pr_html_url` 后 comments 列表出现 `comment-git-pr-reply`，或二次 GET comments 含 `git_pr.html_url`。

### Metadata
- Source: goal-overview
- Related Files: taskCloudService/src/layer_graph_git_pr_backfill.go, taskFE/app/src/composables/taskDetail
- Tags: git-pr-reply, layer-graph, race

---

## [OPT-20260902-029] completed

**Logged**: 2026-09-02
**Priority**: medium
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: scripts/register-precise-restart.sh 增加 --check 进程新鲜度 post-check：反查运行中 pid（/proc cwd + start_command token）比对二进制 mtime ≤ 进程 lstart；taskProjectService 实测 OK，另暴露 task-auth/ai-provider 等 10 个真实 stale 进程
**Area**: infra

### Summary
精准编译重启后断言运行中进程的启动时间不早于新二进制 mtime，避免再出现「磁盘已 rebuild、pid 仍是上午进程」导致 GET git-oauth-grant 405。

### Details
本次 BINDING_MISSING 的直接原因之一是 `taskProjectService` 磁盘二进制 14:24 已含 GET，但 pid 仍是 09:27 只接受 POST。`comment_oauth_l2_repaired` 依赖该 GET。验收：`stat -c %Y taskProjectService/bin/taskProjectService` ≤ `ps -o lstart= -p <pid>` 对应 epoch；或给 `scripts/register-precise-restart.sh` 增加 post-check：`curl -sS -o /dev/null -w '%{http_code}' 'http://127.0.0.1:8016/api/internal/projects/git-oauth-grant/?project_id=p&user_id=u&gitsite=gitlab.example'` 不得为 405。

### Metadata
- Source: goal-overview
- Related Files: scripts/register-precise-restart.sh, taskProjectService/src/main.go
- Tags: precise-restart, stale-process, git-oauth

---

## [OPT-20260902-020] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: 列表/详情 git_repos_status 本地 grant hydrate token_available + createTaskOauthGate 列表 token_available 视为 bound（跳过 validate POST）。Go: 4d17dee; FE: b2ff6aa
**Area**: frontend

### Summary
工作区项目列表接口把 `git_repos_status` 填成 `not_applicable` 占位，创建任务无法用列表缓存判断「已授权」，每次都要再打 `validate-git-repos`。

### Details
`loadProjectDetail` / 列表响应里 `git_repos_status[].token_status = not_applicable`，创建弹窗不能把列表字段当 L2。本次已用「最近打开的项目」+ live validate 对齐详情页。后续可在列表/详情 hydrate 后带上真实 `token_available`（或独立批量接口），创建门禁命中即可跳过一轮 POST。验收：列表 JSON 对已授权仓不再是 `not_applicable`；`createTaskOauthGate` 单测覆盖「列表 token_available 视为 bound」。

### Metadata
- Source: goal-overview
- Related Files: taskProjectService/src/project_response.go, taskFE/app/src/utils/createTaskOauthGate.js
- Tags: oauth, create-task, git_repos_status

---

## [OPT-20260902-023] completed

**Logged**: 2026-09-02
**Priority**: medium
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: zTree BINDING_MISSING chip 旁真实 a[href] 绑定入口（match key→repo_url→start-from-gateway, grant_kind=pending）；Vitest 绿
**Area**: frontend

### Summary
zTree「未绑定 Git 授权」芯片旁增加真实 `a[href]` 绑定入口（gitlab/github-start-from-gateway）。

### Details
本次只把 `BINDING_MISSING` 标成「未绑定 Git 授权」并改 title；用户仍须自己找到 composer / 创建任务入口。层节点已有 origin match key，可拼 `repo_url` + `grant_kind=pending` 真实链接（禁止 `@click` 冒充）。验收：`layer-ztree-push-error-bind` 为 `<a href>`；Vitest 覆盖 GitLab match key。

### Metadata
- Source: goal-overview
- Related Files: taskFE/app/src/utils/layerZtreePushError.js, taskFE/app/src/components/LayerGraphZtreeNode.vue, taskFE/app/src/utils/repoOAuthAuthorizeUtils.js
- Tags: git-oauth, ztree, binding-missing

---

## [OPT-20260902-014] completed

**Logged**: 2026-09-02
**Priority**: low
**Status**: completed
- **Completed**: 2026-09-03
- **Summary**: taskEvents ops 告警 intent：每 order_id 一封邮件；幂等键=order_id；go tests 绿（handler+consumer key+config sync）
**Area**: backend

### Summary
为 `gitlab-manual-node-fulfillment-queued` 增加运维告警消费者（邮件/收信箱），避免只靠 Kafka UI 发现待建节点。

### Details
当前 taskBill 只发布 `GitlabManualNodeFulfillmentQueued`（key=order_id），无 intent worker。应在 taskEvents 增加只读告警 intent：幂等键=order_id，禁止用 tenant_id；DLT=`gitlab-manual-node-fulfillment-queued-dlt`。验收：重放同一 order_id 只告警一次。

### Metadata
- Source: goal-overview
- Related Files: taskBill/src/events.go, taskEvents/cmd
- Tags: kafka, gitlab, ops-alert

---

## [OPT-20260903-005] completed

- **Status**: completed
- **Completed**: 2026-09-03
- **Summary**: loadCloudServerConfigsByIDs IN-batch; TestCommentContainerBindingsToJSONBatchLoadsCSC 3 rows byID=0 batch=3; TestCommentContainerBindingJSONIncludesCSCRuntime exit 0. Search-all also batched instruction_idle / purgeStaleContainerReachability / reconcileTerminalTaskCSCs.
- **Created**: 2026-09-03
- **Context**: 评论 binding 列表 JSON 对每条 `csc_id` 调用 `loadCloudServerConfigByID`（`attachCSCRuntimeOntoBindingJSON`），任务下多评论时变成 N+1。本次为「等待容器」面板必须带上 `last_runtime_status`/`has_server_url`，查询放大是副作用。
- **Action**: (1) 在 `comment_container_bindings_store.go` 把 attach 改为一次 `WHERE id IN (...)` 批量加载 CSC；(2) 给 `commentContainerBindingsToJSON` 走批量路径；(3) 测例用 3 条 binding 断言 `db` Query 次数或结果字段齐全。
- **Why**: 工作面板冷打开会 list 全部评论 binding；N+1 会把启机态查询拖慢，并在 MySQL 连接池上与 start-vm 抢连接。
- **How to apply**: `cd taskCloudService/src && go test -count=1 -run TestCommentContainerBindingJSONIncludesCSCRuntime .` 仍绿；新增批量测例 `TestCommentContainerBindingsToJSONBatchLoadsCSC` exit 0。
- **Completion-Note**: `go test -count=1 -run 'TestCommentContainerBindingJSONIncludesCSCRuntime|TestCommentContainerBindingsToJSONBatchLoadsCSC' .` exit 0；batch hook IN size=3 且 by-ID 调用=0。

## [OPT-20260903-006] completed

- **Status**: completed
- **Completed**: 2026-09-03
- **Summary**: probe public_ip:8080 via DirectClient 1.5s; HTTP ready extends grace; refused fails binding. TestReconcileStaleStartingReachabilitySkipsWhenHTTPReady + FailsWhenPortRefused + TestStaleStartingReachability exit 0.
- **Created**: 2026-09-03
- **Context**: `reconcileStaleStartingReachability` 仅按「Running + 空 server_url + 超时」收口。本会话实例 `i-m5ehqazesr6um99wdii9` 公网 IP 已分配但 8080 拒绝连接，超时判定正确；若未来 UserData 已监听但 register-reachability 失败，20 分钟一刀切会误伤。
- **Action**: (1) 在超时分支对 `public_ip:8080` 做一次短超时 HTTP/TCP 探活；(2) 探活成功则只打 warn + 延长宽限，失败才 `failBindingForStaleReachability`；(3) httptest 测例覆盖「端口通不收口 / 端口拒绝才收口」。
- **Why**: 把「进程没起来」和「登记接口 bug」分开，避免误标 failed 后还要人工恢复 binding。
- **How to apply**: 新增 `TestReconcileStaleStartingReachabilitySkipsWhenHTTPReady` / `...FailsWhenPortRefused`，`go test -count=1 -run TestReconcileStaleStartingReachability ./src` exit 0。
- **Completion-Note**: `go test -count=1 -run 'TestReconcileStaleStartingReachability|TestStaleStartingReachability' .` exit 0；httptest 端口通保持 starting，关闭 listener 后收口 failed。

## [OPT-20260903-013] completed

- **Status**: completed
- **Completed**: 2026-09-04
- **Summary**: workspace.js 加载失败统一经 P.setLoadError 打 data-traceId（成员/进度列/交付物/镜像/个人配置/项目/工作空间）；node --test 589 绿
- **Created**: 2026-09-03
- **Context**: 本次修浮窗工作空间 403「您不是该公司的成员」时，已给 `#taskplugin-workspace` 与 DevTools 工作空间下拉写 `data-traceId`。`panel/lib/workspace.js` 里成员/进度列/交付物/镜像/个人配置/项目列表的 `加载失败` 仍未打该属性。
- **Action**: (1) 在 `panel/lib/workspace.js` 抽出 `setLoadError(el, html, err)` 调用 `setDataTraceId`；(2) 替换其余 `加载失败` 赋值；(3) 源码契约测断言每处 catch 含 `setDataTraceId`。
- **Why**: 元规则要求请求失败 UI 带 `data-traceId`；否则成员/项目加载 403 无法用同一 id 查 Loki。
- **How to apply**: `taskChromePlugin/panel/lib/workspace.js`、`taskChromePlugin/test/panel-auth-badge-workspace.test.js`。验收：`node --test test/panel-auth-badge-workspace.test.js` 全绿且断言 `setDataTraceId`。

## [OPT-20260903-010] completed

- **Status**: completed
- **Completed**: 2026-09-04
- **Summary**: createOrder 对 gitlab_disk 小计=单价×GB×disk_months（校验1..36），行项落盘 disk_months(migration077)；markOrderPaid 用订单行月数不再写死1（commit b83d6e7）
- **Created**: 2026-09-03
- **Context**: 本次改目录价与起购时确认：购买页 `OrderCreate.vue` 用 `price_yuan × GB × months` 预估小计，但 `createOrder` 只做 `unitPriceCents * quantity`，忽略 `disk_months`。改价后 10GB×N 月的展示金额会与实扣分叉。
- **Action**: (1) 写失败测：`disk_months=3`、quantity=10 时期望小计为单价×10×3；(2) 在 `createOrder` 对 `gitlab_disk` 乘月数并校验 1–36；(3) `markOrderPaid` 用订单行月数写 `disk_months`/`disk_expires_at`，不要写死 1；(4) 跑 `go test ./src -count=1 -timeout 180s -run 'TestCreateOrderGitlabDisk'`。
- **Why**: 用户按月选购，实扣却按 1 个月，会造成少收与到期日错误。
- **How to apply**: `taskBill/src/orders.go` 计价循环、`taskBill/src/order_payment.go` 发放磁盘配额；对照 `OrderCreate.vue` 的 `diskSubtotal`。

## [OPT-20260903-012] completed

- **Status**: completed
- **Completed**: 2026-09-04
- **Summary**: 首次购买(pending_admin)支付到期日留空；handleAdminProvisionGitlabResource 开通成功按 disk_months 自开通日起算；已开通续费按现有到期日顺延；含回归测（commit b83d6e7）
- **Created**: 2026-09-03
- **Context**: 手动发货设计仍在 `markOrderPaid` 写 `disk_expires_at`。用户付款后若排队数日才开通，已购月数被排队吃掉。
- **Action**: (1) 支付时只记 `disk_months`、到期日留空或占位；(2) `handleAdminProvisionGitlabResource` 成功时按月数计算 `disk_expires_at`；(3) 已 active 加购按开通/支付策略补测；(4) `go test ./src -count=1 -timeout 180s -run 'TestAdminProvision|TestMarkOrderPaidGitlabDisk'`。
- **Why**: 手动开通后「买 1 个月只用到排队剩余天数」不合理。
- **How to apply**: `taskBill/src/order_payment.go`、`gitlab_region.go` provision 更新语句。

## [OPT-20260903-014] completed

- **Status**: completed
- **Completed**: 2026-09-04
- **Summary**: 机器摘要「!」详情支持触屏点击展开、点击外部/Esc 关闭（taskFE）
- **Created**: 2026-09-03
- **Context**: WorkPanel 机器摘要改为 hover 展示详情（`WorkspaceMachineSummary.vue`），移动端无 hover，用户看不到图例与闲置回收说明。
- **Action**: 为 `workspace-machine-summary-info` 增加 click/tap 切换 `detailTipOpen`，并在点击外部或 Esc 时关闭；补 vitest 与 Playwright 触屏路径。
- **Why**: 纯 hover 浮层在平板/手机不可达，违背可访问性与跨端一致。
- **How to apply**: `taskFE/app/src/components/WorkspaceMachineSummary.vue`；验收：新增单测 + 可选 Playwright `tap` 断言 tooltip 可见。

## [OPT-20260903-008] completed

- **Status**: completed
- **Completed**: 2026-09-04
- **Summary**: 进度错误横幅 #prog-error 带 run_id 写 data-traceId 供 Loki 关联（runAll）
- **Created**: 2026-09-03
- **Context**: 本次修复了 `#prog-error` 长文案被 `.start-all-progress { overflow: hidden }` 裁切，并给 `title` 放全文。SSE 进度错误没有 HTTP `X-Trace-Id`，排障仍无法从横幅跳到 Loki。
- **Action**: (1) 进度事件带上 `run_id`；(2) `updateProgress` 在 `ev.error` 时给 `#prog-error` 写 `data-traceId`（仅当 run_id/trace 非空，禁止 `"unknown"`）；(3) 源码契约测试断言赋值存在。
- **Why**: 批量启停失败时运维只能看到截断/全文错误字符串，无法用同一 id 检索 runAll 日志。
- **How to apply**: `runAll/src/status_ui/js/02.js` 的 `updateProgress`；进度 SSE 结构体。验收：`node --test runAll/src/status_ui/js/progress_error_display.test.js` 全绿，且测例断言 `data-traceId`。

## [OPT-20260903-009] completed

- **Status**: completed
- **Completed**: 2026-09-04
- **Summary**: 允许显式单服务启动从 Retrying 状态回收重新拉起（runAll）
- **Created**: 2026-09-03
- **Context**: 本次修了 canary overlap 失败后 `restartService` 卡在 `retrying, cannot start`。`startService` / `ManagedService.CanStart` / `IsStartableServiceStatus` 仍把 Retrying 当非空闲，UI「启动」对 Retrying 仍返回 `can only start idle services`。
- **Action**: (1) 给 `IsStartableServiceStatus` 加 Retrying 的回归测（`managed_service_entity_test.go`）；(2) 决定是允许 Start 从 Retrying CAS 到 Starting，还是 UI 对 Retrying 只展示重启；(3) 若允许 Start，补 `TestRunner_StartService_FromRetrying`。
- **Why**: 运维在 Status 页对卡住的 Retrying 点「启动」仍失败，只能靠「重启」；两按钮语义不一致。
- **How to apply**: `runAll/src/domain/managed_service_entity.go`、`runAll/src/runner.go` 的 `startService`。验收：`go test ./src -count=1 -timeout 60s -run 'TestStartService_FromRetrying|TestManagedService_CanStart'`。

## [OPT-20260903-007] completed

- **Status**: completed
- **Completed**: 2026-09-04
- **Summary**: 拆分 runner.go 健康收尾与 restartService 主体，行数 4919→4715（runAll）
- **Created**: 2026-09-03
- **Context**: 本次把 restart 状态门闩抽到 `runner_restart_claim.go`，但 `runner.go` 仍远超 500 行门禁。继续往该文件堆功能会让启动/重启/健康检查更难审。
- **Action**: (1) 将 `startAndCheck` 健康收尾（Retrying CAS / superseded）再抽到 `runner_startup_health.go`；(2) 将 `restartService` 主体迁到 `runner_restart_service.go`；(3) 每抽一次跑 `gofmt` + `go test ./src -count=1 -timeout 180s -run 'TestRestart|TestClaimServiceRestart|TestFailedService'`。
- **Why**: 超标巨石文件使并发重启与健康检查竞态难以定位；不拆则下次改重启路径仍会误伤。
- **How to apply**: `runAll/src/runner.go`、新建 `runner_startup_health.go` / `runner_restart_service.go`。验收：`wc -l runAll/src/runner.go` 下降且上述测试全绿。

## [OPT-20260904-004] completed

- **Status**: completed
- **Completed**: 2026-09-04
- **Summary**: Implemented: openModal loads gitlab-resources; loadRemoteRepos always sends gitlab_host; multi-region select; Vitest+Playwright updated.
- **Created**: 2026-09-04
- **Context**: 已修复后端空 `gitlab_host` 时用 WebsiteOrigin 换票；首屏仍依赖 fallback。多 GitLab provider 租户下 fallback 可能不是用户期望的站点。
- **Action**: (1) `loadRemoteRepos` 在已知 `gitlabWebsite` 时附加 `gitlab_host` query；(2) 模态增加站点选择时同步该参数；(3) 补 Vitest。
- **Why**: 多站点时避免静默打到错误 GitLab；与显式 host 路径一致。
- **How to apply**: `taskFE/app/src/composables/useGitlabProjectSync.js`；对照 `handleGitlabRemoteRepos` 的 `gitlab_host`。

## [OPT-20260904-005] completed

- **Status**: completed
- **Completed**: 2026-09-05
- **Summary**: taskFE 85bcf99：useGitlabProjectSync 按 tenantId 持久化 gitlab-sync-region 到 localStorage，重开同步模态恢复上次区域。Vitest 15 全绿。
- **Created**: 2026-09-04
- **Context**: 已实现打开同步模态时拉取已购区域并默认选列表首项（按 updated_at）；多区域用户每次打开可能要重选常用实例。
- **Action**: (1) 按 `tenantId` 将 `selectedRegionSlug` 写入 localStorage；(2) `loadPurchasedRegions` 优先恢复仍有效的 slug；(3) 补 Vitest。
- **Why**: 减少多区域租户重复选择。
- **How to apply**: `taskFE/app/src/composables/useGitlabProjectSync.js`；键名如 `gitlab-sync-region:${tenantId}`。

## [OPT-20260904-003] completed

- **Status**: completed
- **Completed**: 2026-09-05
- **Summary**: taskFE 74bb7be：GitLab 同步「前往 GitLab 授权」改真实 <a href>（gitlabOAuthStartHref），删除 fetch+location.href 路径。Vitest 16 全绿。
- **Created**: 2026-09-04
- **Context**: 本次已修复 `Accept: application/json` 契约，使 fetch+JSON+`location.href` 可用；仓库内其它 Git OAuth 入口多用原生 `<a href>`（后端对 HTML Accept 做 302）。
- **Action**: (1) 用 `buildRepoOAuthStartHref` / 等价参数构造同步弹窗授权 href；(2) 将「前往 GitLab 授权」改为真实 `<a>`；(3) 删除 fetch 启动路径或保留仅作降级；(4) 更新 Vitest。
- **Why**: 与全站 OAuth 导航模式一致，减少 Accept 契约类回归面。
- **How to apply**: `GitlabSyncProjectsModal.vue` + `useGitlabProjectSync.js`；对照 `repoOAuthAuthorizeUtils.buildRepoOAuthStartHref`。

## [OPT-20260904-002] completed

- **Status**: completed
- **Completed**: 2026-09-05
- **Summary**: taskFE 285e934：镜像市场厂商申请表支持 ?vendor_apply=1 深链展开，展开/收起 router.replace 同步 query，无邮箱不强制展开。ImageMarket Vitest 22 全绿。
- **Created**: 2026-09-04
- **Context**: 厂商申请表单改为点击 CTA 后展开；当前展开态仅存在内存 `vendorApplyFormOpen`，刷新或分享链接会回到收起态，客服/E2E 不便直达填表页。
- **Action**: (1) 约定 query 如 `?vendor_apply=1`；(2) `ImageMarket.vue` 在 `canOpenVendorApplyForm` 时读取并设 `vendorApplyFormOpen`；(3) 展开时可选 `router.replace` 同步 query；(4) 补 Vitest。
- **Why**: 审核驳回后用户刷新会丢表单上下文；深链便于回归与客服指引。
- **How to apply**: `taskFE/app/src/views/ImageMarket.vue`；`vue-router` `route.query`；勿在无邮箱态强制展开。

## [OPT-20260904-011] completed

- **Status**: completed
- **Completed**: 2026-09-05
- **Summary**: taskAuth 5df3863 + conf 2c7134f：新增 runall_ssh_sh_wechat_mp_egress.sh 包装脚本，conf/runAll.yaml 注册 host-sh 组 wechat-mp-egress（healthz 1.117.67.121:8030）。
- **Created**: 2026-09-04
- **Context**: egress 已用 systemd 部署在 sh:8030，但 9999 无对应 start/stop/health 条目（对照 git-service-tencent-sh-1 的 runall_ssh_sh 模式）。
- **Action**: 在 `conf/runAll.yaml` 增加 `wechat-mp-egress`（ssh 探活 `http://1.117.67.121:8030/healthz` + start/stop 包装脚本），避免仅靠手工 systemctl。
- **Why**: 运维可见性与恢复路径应与现有远端组件一致。
- **How to apply**: 仿 `runall_ssh_sh_gitlab.sh`；健康 URL 抄 conf SSOT 端口 8030。

## [OPT-20260904-001] completed

- **Status**: completed
- **Completed**: 2026-09-05
- **Summary**: 审计确认 24 个登记子仓 gitlab remote 与 origin(github) 完全重复；新增 scripts/normalize-gitlab-remotes.sh 报告/修复/门禁三模式并本地执行 --fix 删除 24 个冗余 remote（origin/gitlab-sh-1/8 个正确 gitlab.daydaymoney.com 均未动）；.gitmodules 头部补 remote 命名约定注释。验收: bash scripts/normalize-gitlab-remotes.sh --check 退出码 0（dup=0）。
- **Created**: 2026-09-04
- **Context**: 清理非 main 分支时发现大量子仓的 `gitlab` remote 实际指向 `github.com:task2money/...`（与 `origin` 重复），而 meta / taskAIComment 等则指向 `gitlab.daydaymoney.com`；`gitlab-sh-1` 才是腾讯侧 GitLab。删 `sdk` 的 `gitlab/master` 时出现「远程引用不存在」即因此重复。
- **Action**: (1) 列出全部子仓 `git remote get-url gitlab` 与期望主机对照表；(2) 决定命名约定（真 GitLab vs 别名）；(3) 按约定批量 `git remote set-url` 或删除冗余 remote；(4) 更新 clone/bootstrap 文档以免再写错。
- **Why**: 同名 remote 指向不同主机易导致删分支/推送打到错误端，且浪费重复 fetch。
- **How to apply**: 各子仓 `.git/config` remote 段；对照 `gitlab.daydaymoney.com` 与 `gitlab-tencent-sh-1.daydaymoney.com:2223`；勿改已正确的 meta/`gitlab-sh-1`。

## [OPT-20260904-006] completed

- **Status**: completed
- **Completed**: 2026-09-05
- **Summary**: taskAuth a95fdfd + taskFE cf7270d：定位 :4000 activate-session 500 db error 根因为 token client_ip 网关 docker-bridge(172.25.0.1) vs :4000 nginx loopback(127.0.0.1) 不一致；修复 errTokenIPMismatch→401 + loopback 同机容差 + helper 优先 siteOrigin 回退 gateway；Go 单测绿。live 已验证：canary 重建 taskAuth（新实例 pid 3220503，source+deploy-root bin 均更新），:4000 activate-session 绑定 bridge IP token 返回 200（此前 500 db error）。
- **Created**: 2026-09-04
- **Context**: Playwright 经 `http://127.0.0.1:4000/api/accounts/users/activate-session/` 恒 500 `db error`；直打网关 `:18081` 则 200。E2E 现绕行 gateway + 镜像 cookie 到 siteOrigin。
- **Action**: (1) 用 trace_id 查 Loki/APISIX/nginx 定位 500；(2) 修复本机 :4000 反代/上游 DB 会话；(3) 恢复 `loginViaGatewayApi` 优先走 siteOrigin 激活以便 Set-Cookie 自然落在前端域。
- **Why**: 本机同源激活失败会迫使 E2E 手工镜像 cookie，且 UI 登录若走同路径也会踩坑。
- **How to apply**: 边缘 nginx / APISIX `/api`；对照 `gatewayLoginE2e.js` 注释。

## [OPT-20260904-008] completed

- **Status**: completed
- **Completed**: 2026-09-05
- **Summary**: live 验证通过(2026-09-05): FE 已提升至 release 20260904190805(含 897556b/741f302/fd55001), :4000 服务运行 taskProjectService bin 19:07 构建含 a7d4820。Playwright 登录 E2E 账号, 创建任务弹窗输入中文标题"自动验收631标题翻译测试", POST /api/projects/translate-branch-title/tenant_id/882297276515512320/ 200 {used_ai:true, translated_title:auto-acceptance-631-title-translation-test}; 工作分支自动填充 feature/2026-09-05____daydaymoney${taskId}_auto-acceptance-631-title-translation-test, 无红字。验证脚本 playwright/opt-20260904-008-verify.cjs
- **Created**: 2026-09-04
- **Context**: Loki `487b9895-1d9b-40fb-b093-b624d78432d3`：deepseek-v4-flash 默认 thinking 占满 max_tokens=128 → 空 content；已修请求 `thinking.type=disabled`，并对用户展示分类失败原因。
- **Action**: (1) http://10.2.150.68:9999/ 「精准编译重启」task-project-service + taskFE；(2) 打开工作台创建任务，输入中文标题；(3) 成功时应自动出英文分支片段；若仍失败，红字须含具体原因（超时/未返回可用译文等）+「本地规则」+ data-traceId。
- **Why**: 需运行时确认线上服务与前端 bundle 已加载修复。
- **How to apply**: 精准重启登记已写入 `.runall/precise_restart_services.txt`。

## [OPT-20260905-001] completed

- **Status**: completed
- **Completed**: 2026-09-05
- **Summary**: SOURCE vite build 20260905074451 + rsync to daydaymoney-deploy public/html + nginx reload; verified https://www.daydaymoney.com/static/assets/Home-Bqc-7ZlT.js contains 闽ICP备19027308号-4 and home-footer-icp
- **Created**: 2026-09-05
- **Context**: 首页页脚已改为从 `conf/frontend/vue/config.yaml` 的 `icpBeian` 经 Vite `VITE_ICP_BEIAN` 注入；源码与单测已绿，但公网 SPA 仍为旧 bundle。
- **Action**: (1) 在 http://10.2.150.68:9999/ 点击「精准编译重启」taskFE（已登记 `.runall/precise_restart_services.txt`）；(2) 打开 https://www.daydaymoney.com/ 页脚确认显示「闽ICP备19027308号-4」且链接至 beian.miit.gov.cn。
- **Why**: Vite define 为构建期注入，不重启/不重建则线上看不到备案号。
- **How to apply**: `scripts/register-precise-restart.sh taskFE`；验收选择器 `a[data-testid=home-footer-icp-link]`。

## [OPT-20260905-005] completed

- **Status**: completed
- **Completed**: 2026-09-05
- **Summary**: persisted .runall/last_orchestrator_exit.json; watchdog alert includes last_exit; runall-console keep-tail 256KiB
- **Created**: 2026-09-05
- **Context**: 诊断 `http://10.2.150.68:9999/` 不可达时，`logs/runall-console.log` 已被 08:00 log-truncate 清空，无法从控制台日志还原编排器退出原因；仅能靠 watchdog 告警确认「UI down + no_restart」。
- **Action**: (1) runAll 在收到 SIGTERM/SIGINT/fatal exit 前将 `exit_reason`/`signal`/`pid`/`time` 追加写入不被 truncate 的小文件（如 `.runall/last_orchestrator_exit.json`）；(2) watchdog 告警文案附带该文件摘要；(3) 评估 truncate 对 `runall-console.log` 是否应保留尾部 N KB。
- **Why**: `:9999` 空窗可反复出现；无退出指纹时只能「进程没了」而无法区分人为停、信号误杀、崩溃。
- **How to apply**: `runAll/src/main.go` / signal handlers；`scripts` 下 service-watchdog；`logs/log-truncate` 白名单或尾部保留策略。

