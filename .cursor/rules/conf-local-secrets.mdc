---
description: 机密参数只能放在 conf-local，已跟踪 conf YAML 禁止非空密钥
alwaysApply: true
---

# 机密参数仅允许放在 conf-local（元规则）

适用于整个 monorepo 与 `daydaymoney-deploy` clone-run。细则见 `.ai/01_project_constraints/63_conf_local_secrets_only.md`；约束索引第 58 条；ADR-0054。

## 强制要求

- **`conf/`**：仅非机密参数（端口、URL、开关、空字符串骨架）
- **`conf-local/`**：与 `conf/` 同相对路径；gitignore；放 secretId / secretKey / Token / client secret / internalSecret / PEM / runAll env
- 加载：`confload` / `conf_loader` 只合 `conf/<rel>` → `conf-local/<rel>`；不读 `config.local.yaml`
- 禁止再维护 HOST_SECRETS Markdown 登记册
- 已跟踪 YAML 中的凭据必须抽空；迁出后轮换（第 56 条）

## 触发

- 新增云/支付/OAuth/SMS/COS/TLS/OIDC 密钥
- 改 `confload`、`conf_loader`、`conf-sync`、`up.sh`

## 验收

```bash
python3 db/scripts/ci/test_check_conf_local_secrets.py
python3 db/scripts/ci/check_conf_local_secrets.py
```
