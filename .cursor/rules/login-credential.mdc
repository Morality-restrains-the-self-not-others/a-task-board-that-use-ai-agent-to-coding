---
description: 登录凭证提示词 - Playwright 登录 localhost:4000/auth/login/ 的复用配置
globs: "**/playwright*.js,**/playwright*.mjs,**/tests/**/*login*"
alwaysApply: false
---

# 登录凭证提示词

## 开发环境登录配置

### 登录地址
- **URL**: `http://localhost:4000/auth/login/`

### 测试账号凭证
- **邮箱**: `process.env.PLAYWRIGHT_TEST_EMAIL`（缺省测试身份见 `task2app/测试.ai.md`）
- **密码**: **只**读 `process.env.PLAYWRIGHT_TEST_PASSWORD`；禁止把口令写进规则、源码或 `env || '明文'` 默认值（元规则第 57 条）

## Playwright 登录复用方法

### 使用封装函数
```javascript
import { playwrightLoginWithLegalAccept } from './playwrightLogin.js';

const password = process.env.PLAYWRIGHT_TEST_PASSWORD;
if (!password) {
  throw new Error('PLAYWRIGHT_TEST_PASSWORD is required');
}

await playwrightLoginWithLegalAccept(page, {
  email: process.env.PLAYWRIGHT_TEST_EMAIL || 'contact@daydaymoney.com',
  password,
});
```

### 核心步骤说明
1. 导航到 `/auth/login/`
2. 切换到「邮箱/密码」登录标签（如存在）
3. 勾选隐私协议和许可协议
4. 填写邮箱和密码
5. 提交表单并等待页面跳转

### 注意事项
- 登录前需确保页面 DOM 完全加载
- 表单提交后等待 URL 不再包含 `/auth/login`
- 预留足够的等待时间（建议 60 秒超时）
