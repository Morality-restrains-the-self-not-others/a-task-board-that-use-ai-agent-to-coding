---
description: 密钥必须放在 conf/环境变量/密钥系统，禁止硬编码在源码中
alwaysApply: true
---

# 密钥禁止硬编码在源码中（元规则）

适用于整个 monorepo。细则见 `.ai/01_project_constraints/62_no_hardcoded_secrets.md`；约束索引第 57 条；ADR-0046。

## 强制要求

- **禁止**把 API key、secret、password、token、私钥等凭据写成源码字面量
- **必须**存放在 gitignored 的 `conf-local/`（与 `conf/` 同相对路径）、环境变量或 KMS/CI secret store；已跟踪 `conf/` 不得含非空机密键
- **禁止**提交 `.env` / `.env.local`（`.env.example` 除外）
- 测试凭据只读环境变量；禁止 `env || '真实口令'` 默认值
- 日志与错误响应不得输出完整密钥
- 公开客户端等无法保密的值须标注 `Secret-Hardcode-OK: <原因>`
- 密钥已进入已推送历史 → 按第 56 条废弃并重新生成，禁止 filter-repo
- 新增/变更密钥时，须同时遵守第 58 条（只写入 `conf-local/`）

## 触发

- 新增认证/支付/云 SDK/OAuth/Webhook/数据库连接代码
- 编写 Playwright、调试脚本、本地登录工具
- pre-commit 报 hardcoded secrets 门禁失败

## 验收

```bash
python3 db/scripts/ci/test_check_no_hardcoded_secrets.py
python3 db/scripts/ci/check_no_hardcoded_secrets.py
```
