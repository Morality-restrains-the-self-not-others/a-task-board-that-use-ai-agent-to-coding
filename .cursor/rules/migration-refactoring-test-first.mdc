---
description: 迁移/重构类指令——测试先行，确保改动前后行为一致。全会话 alwaysApply，不限制 globs。
alwaysApply: true
---

# 迁移/重构——测试先行（元规则）

适用于整个 monorepo。详细条文见 `.ai/01_project_constraints/30_migration_refactoring_test_first.md`。

## 强制要求

- **识别**：收到用户指令后，对照触发词/模式判定是否属于迁移/重构类。一级触发词：迁移/migrate、重构/refactor、重写/rewrite、搬迁/relocate、替换/replace、升级/upgrade、抽取/extract、合并/merge、拆分/split。二级触发模式：「把 X 移到 Y」「用 Y 重新实现 X」「更改底层存储/协议」等。
- **测试先行**：确认触发后，**禁止直接执行代码改动**。必须先：
  1. 定位受影响代码的公开接口
  2. 检查现有测试覆盖 → 不足则写 characterization test（基线测试）
  3. 用测试建立当前行为基线（记录实际行为，非理想行为）
- **改动中验证**：迁移/重构过程中，每步改动后立即跑相关测试，失败即停修
- **完成后验证**：全量测试 + CI 门禁通过，方可视为完成
- **不确定时**：宁可多写测试，不可跳过

## 触发

每次收到用户指令时自动判定。若指令包含迁移/重构特征但 Agent 不确定是否触发：默认触发（多写测试无害）。

## 排除

修复单 bug、新增独立功能、纯格式化、加日志/注释、单文件 < 20 行有效变更且不改接口签名。
