---
description: 平台 GitLab 禁止自行注册；Web 登录仅允许 taskAuth OIDC SSO；禁止打开 signup/账密排障
alwaysApply: true
---

# GitLab 禁止自行注册、仅允许 SSO（元规则）

适用于平台 GitLab 服务（`gitService` 及全部 `conf/infra/git-service*` 区域实例）。细则见 `.ai/01_project_constraints/55_gitlab_sso_only_no_self_signup.md`；约束索引第 50 条；ADR-0016。

## 强制要求

- **禁止**用户经 GitLab `/users/sign_up` 自行注册；`signupEnabled` 必须显式 `false`
- Web 登录**仅允许** taskAuth OIDC（OmniAuth `openid_connect`）；`passwordAuthWeb` / `passwordAuthGit` 必须 `false`
- 首次 SSO 可自动开通 GitLab 用户（`omniauth_block_auto_created_users = false`）；这不是自行注册
- 排障禁止把注册或账密改回 `true`；须修 OIDC / OmniAuth
- 新增区域实例必须带齐上述三键且为 `false`；启动须走 `apply_auth_policy.sh`（DB 级设置）

## 触发

- 改 `conf/infra/git-service*/config.yaml`、`gitService/docker-compose.yml`、`run.sh`、OIDC/OmniAuth
- 「GitLab 登不上」拟开启注册或账密
- 新增 GitLab 区域（ADR-0014）

## 验收

```bash
python3 db/scripts/ci/test_check_gitlab_sso_only_no_self_signup.py
python3 db/scripts/ci/check_gitlab_sso_only_no_self_signup.py
```
