bin/
.trajectories/
*.log
.DS_Store
go.work
go.work.sum

# Misnamed go build output when cwd is src/ (use bin/ instead)
/src/src
# 本地配置（含 API 密钥）— 不入库；回退 claude_config.yaml.example
claude_config.yaml
# Secret env files (meta gitignore parity — OPT-20260827-017)
.env
.env.*
!.env.example
!.env.sample
!.env.template