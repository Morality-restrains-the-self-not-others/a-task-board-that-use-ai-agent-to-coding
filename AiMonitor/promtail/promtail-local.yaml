# Auto-generated by runAll/scripts/generate-promtail-config.sh
# DO NOT EDIT MANUALLY — regenerate: bash runAll/scripts/generate-promtail-config.sh
# Canonical path: /var/log/runall/<runAll-service-name>.log
# Alias paths (*.restart.log / *-run.log) map to the same job to reduce bypass漏采.
server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /var/lib/promtail/positions.yaml

clients:
  - url: ${LOKI_PUSH_URL}

scrape_configs:
  - job_name: ai-monitor
    static_configs:
      - targets:
          - localhost
        labels:
          job: ai-monitor
          __path__: /var/log/runall/ai-monitor.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: ai-provider
    static_configs:
      - targets:
          - localhost
        labels:
          job: ai-provider
          __path__: /var/log/runall/ai-provider.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: docker-kafka
    static_configs:
      - targets:
          - localhost
        labels:
          job: docker-kafka
          __path__: /var/log/runall/docker-kafka.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: docker-mysql
    static_configs:
      - targets:
          - localhost
        labels:
          job: docker-mysql
          __path__: /var/log/runall/docker-mysql.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: docker-portainer
    static_configs:
      - targets:
          - localhost
        labels:
          job: docker-portainer
          __path__: /var/log/runall/docker-portainer.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: docker-redis
    static_configs:
      - targets:
          - localhost
        labels:
          job: docker-redis
          __path__: /var/log/runall/docker-redis.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: git-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: git-service
          __path__: /var/log/runall/git-service.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: git-service-tencent-sh-1
    static_configs:
      - targets:
          - localhost
        labels:
          job: git-service-tencent-sh-1
          __path__: /var/log/runall/git-service-tencent-sh-1.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: go-relay
    static_configs:
      - targets:
          - localhost
        labels:
          job: go-relay
          __path__: /var/log/runall/go-relay.log
      - targets:
          - localhost
        labels:
          job: go-relay
          __path__: /var/log/runall/go_relayToTrae.restart.log
      - targets:
          - localhost
        labels:
          job: go-relay
          __path__: /var/log/runall/go-relay/relay.restart.log
      - targets:
          - localhost
        labels:
          job: go-relay
          __path__: /var/log/runall/go-relay/relay.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: promtail-local
    static_configs:
      - targets:
          - localhost
        labels:
          job: promtail-local
          __path__: /var/log/runall/promtail-local.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: runall
    static_configs:
      - targets:
          - localhost
        labels:
          job: runall
          __path__: /var/log/runall/runall.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-agent-support
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-agent-support
          __path__: /var/log/runall/task-agent-support.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-ai-comment
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-ai-comment
          __path__: /var/log/runall/task-ai-comment.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-ai-endpoint
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-ai-endpoint
          __path__: /var/log/runall/task-ai-endpoint.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-auth
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-auth
          __path__: /var/log/runall/task-auth.log
      - targets:
          - localhost
        labels:
          job: task-auth
          __path__: /var/log/runall/task-auth/task-auth.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-bill
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-bill
          __path__: /var/log/runall/task-bill.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-cloud-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-cloud-service
          __path__: /var/log/runall/task-cloud-service.log
      - targets:
          - localhost
        labels:
          job: task-cloud-service
          __path__: /var/log/runall/task-cloud-service-run.log
      - targets:
          - localhost
        labels:
          job: task-cloud-service
          __path__: /var/log/runall/task-cloud-service/task-cloud-service.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-container-gateway
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-container-gateway
          __path__: /var/log/runall/task-container-gateway.log
      - targets:
          - localhost
        labels:
          job: task-container-gateway
          __path__: /var/log/runall/taskContainerGateway.restart.log
      - targets:
          - localhost
        labels:
          job: task-container-gateway
          __path__: /var/log/runall/task-container-gateway/gateway.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-credential-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-credential-service
          __path__: /var/log/runall/task-credential-service.log
      - targets:
          - localhost
        labels:
          job: task-credential-service
          __path__: /var/log/runall/taskCredentialService.restart.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-ai-assistant-reply-completed-1-persist-assistant-reply
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-ai-assistant-reply-completed-1-persist-assistant-reply
          __path__: /var/log/runall/task-events-ai-assistant-reply-completed-1-persist-assistant-reply.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-billing-order-comment-created-1-notify-other-side
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-billing-order-comment-created-1-notify-other-side
          __path__: /var/log/runall/task-events-billing-order-comment-created-1-notify-other-side.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-billing-profit-sharing-scan-1-process-pending
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-billing-profit-sharing-scan-1-process-pending
          __path__: /var/log/runall/task-events-billing-profit-sharing-scan-1-process-pending.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-billing-referral-settle-scan-1-settle-due
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-billing-referral-settle-scan-1-settle-due
          __path__: /var/log/runall/task-events-billing-referral-settle-scan-1-settle-due.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-billing-transaction-created-1-process-billing-transaction
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-billing-transaction-created-1-process-billing-transaction
          __path__: /var/log/runall/task-events-billing-transaction-created-1-process-billing-transaction.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-billing-transaction-created-2-mark-user-tenant
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-billing-transaction-created-2-mark-user-tenant
          __path__: /var/log/runall/task-events-billing-transaction-created-2-mark-user-tenant.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-cloud-csc-reconcile-1-sweep
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-cloud-csc-reconcile-1-sweep
          __path__: /var/log/runall/task-events-cloud-csc-reconcile-1-sweep.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-cloud-platform-authorization-created-1-process-cloud-platform-authorization
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-cloud-platform-authorization-created-1-process-cloud-platform-authorization
          __path__: /var/log/runall/task-events-cloud-platform-authorization-created-1-process-cloud-platform-authorization.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-cloud-server-start-auto-1-process-server-start-auto
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-cloud-server-start-auto-1-process-server-start-auto
          __path__: /var/log/runall/task-events-cloud-server-start-auto-1-process-server-start-auto.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-cloud-server-started-1-process-server-start
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-cloud-server-started-1-process-server-start
          __path__: /var/log/runall/task-events-cloud-server-started-1-process-server-start.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-cloud-server-stopped-1-process-server-stop
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-cloud-server-stopped-1-process-server-stop
          __path__: /var/log/runall/task-events-cloud-server-stopped-1-process-server-stop.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-company-created-1-set-default-deliverable-system
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-company-created-1-set-default-deliverable-system
          __path__: /var/log/runall/task-events-company-created-1-set-default-deliverable-system.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-company-created-2-set-default-progress-system
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-company-created-2-set-default-progress-system
          __path__: /var/log/runall/task-events-company-created-2-set-default-progress-system.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-company-created-3-create-default-workspace
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-company-created-3-create-default-workspace
          __path__: /var/log/runall/task-events-company-created-3-create-default-workspace.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-company-created-5-init-tenant-feature-params
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-company-created-5-init-tenant-feature-params
          __path__: /var/log/runall/task-events-company-created-5-init-tenant-feature-params.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-company-created-6-grant-initial-resources
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-company-created-6-grant-initial-resources
          __path__: /var/log/runall/task-events-company-created-6-grant-initial-resources.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-container-migrate-await-ready-1-await-running-heartbeat
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-container-migrate-await-ready-1-await-running-heartbeat
          __path__: /var/log/runall/task-events-container-migrate-await-ready-1-await-running-heartbeat.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-email-invite-expiry-scan-1-expire
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-email-invite-expiry-scan-1-expire
          __path__: /var/log/runall/task-events-email-invite-expiry-scan-1-expire.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-email-sent-1-send-email
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-email-sent-1-send-email
          __path__: /var/log/runall/task-events-email-sent-1-send-email.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-gitlab-manual-node-fulfillment-queued-1-ops-alert
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-gitlab-manual-node-fulfillment-queued-1-ops-alert
          __path__: /var/log/runall/task-events-gitlab-manual-node-fulfillment-queued-1-ops-alert.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-invitation-created-1-send-invitation-email
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-invitation-created-1-send-invitation-email
          __path__: /var/log/runall/task-events-invitation-created-1-send-invitation-email.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-member-joined-1-create-default-git-identity
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-member-joined-1-create-default-git-identity
          __path__: /var/log/runall/task-events-member-joined-1-create-default-git-identity.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-oidc-sso-idempotency-cleanup-1-cleanup
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-oidc-sso-idempotency-cleanup-1-cleanup
          __path__: /var/log/runall/task-events-oidc-sso-idempotency-cleanup-1-cleanup.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-project-deleted-1-detach-task-projects
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-project-deleted-1-detach-task-projects
          __path__: /var/log/runall/task-events-project-deleted-1-detach-task-projects.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-project-revision-recorded-1-observe
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-project-revision-recorded-1-observe
          __path__: /var/log/runall/task-events-project-revision-recorded-1-observe.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-project-updated-1-process-project-update
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-project-updated-1-process-project-update
          __path__: /var/log/runall/task-events-project-updated-1-process-project-update.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-queued-auto-run-scan-1-dispatch
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-queued-auto-run-scan-1-dispatch
          __path__: /var/log/runall/task-events-queued-auto-run-scan-1-dispatch.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-referral-code-expiry-scan-1-expire
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-referral-code-expiry-scan-1-expire
          __path__: /var/log/runall/task-events-referral-code-expiry-scan-1-expire.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-registration-invite-1-observability
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-registration-invite-1-observability
          __path__: /var/log/runall/task-events-registration-invite-1-observability.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-relay-lifecycle-1-append-token-audit
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-relay-lifecycle-1-append-token-audit
          __path__: /var/log/runall/task-events-relay-lifecycle-1-append-token-audit.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-relay-lifecycle-2-open-runtime-session
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-relay-lifecycle-2-open-runtime-session
          __path__: /var/log/runall/task-events-relay-lifecycle-2-open-runtime-session.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-relay-lifecycle-3-clear-reachability
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-relay-lifecycle-3-clear-reachability
          __path__: /var/log/runall/task-events-relay-lifecycle-3-clear-reachability.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-relay-lifecycle-4-relay-workflow-update
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-relay-lifecycle-4-relay-workflow-update
          __path__: /var/log/runall/task-events-relay-lifecycle-4-relay-workflow-update.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-sse-message-1-send-sse-message
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-sse-message-1-send-sse-message
          __path__: /var/log/runall/task-events-sse-message-1-send-sse-message.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-sse-message-2-persist-job-execution-event
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-sse-message-2-persist-job-execution-event
          __path__: /var/log/runall/task-events-sse-message-2-persist-job-execution-event.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-task-comment-image-mentioned-1-start-vm-for-at-mention
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-task-comment-image-mentioned-1-start-vm-for-at-mention
          __path__: /var/log/runall/task-events-task-comment-image-mentioned-1-start-vm-for-at-mention.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-task-completed-1-process-task-completion
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-task-completed-1-process-task-completion
          __path__: /var/log/runall/task-events-task-completed-1-process-task-completion.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-task-created-1-fanout-work-panel-sse
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-task-created-1-fanout-work-panel-sse
          __path__: /var/log/runall/task-events-task-created-1-fanout-work-panel-sse.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-task-created-2-create-task-post
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-task-created-2-create-task-post
          __path__: /var/log/runall/task-events-task-created-2-create-task-post.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-task-deleted-1-fanout-work-panel-sse
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-task-deleted-1-fanout-work-panel-sse
          __path__: /var/log/runall/task-events-task-deleted-1-fanout-work-panel-sse.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-task-graceful-shutdown-await-1-hard-release-on-timeout
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-task-graceful-shutdown-await-1-hard-release-on-timeout
          __path__: /var/log/runall/task-events-task-graceful-shutdown-await-1-hard-release-on-timeout.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-task-post-expired-1-notify
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-task-post-expired-1-notify
          __path__: /var/log/runall/task-events-task-post-expired-1-notify.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-task-post-expiry-scan-1-expire-posts
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-task-post-expiry-scan-1-expire-posts
          __path__: /var/log/runall/task-events-task-post-expiry-scan-1-expire-posts.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-task-post-renewed-1-notify
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-task-post-renewed-1-notify
          __path__: /var/log/runall/task-events-task-post-renewed-1-notify.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-task-revision-recorded-1-observe
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-task-revision-recorded-1-observe
          __path__: /var/log/runall/task-events-task-revision-recorded-1-observe.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-task-status-changed-1-release-servers-on-terminal
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-task-status-changed-1-release-servers-on-terminal
          __path__: /var/log/runall/task-events-task-status-changed-1-release-servers-on-terminal.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-task-status-changed-2-fanout-work-panel-sse
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-task-status-changed-2-fanout-work-panel-sse
          __path__: /var/log/runall/task-events-task-status-changed-2-fanout-work-panel-sse.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-user-account-deletion-execute-scan-1-execute-due
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-user-account-deletion-execute-scan-1-execute-due
          __path__: /var/log/runall/task-events-user-account-deletion-execute-scan-1-execute-due.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-user-activated-1-send-welcome-notification
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-user-activated-1-send-welcome-notification
          __path__: /var/log/runall/task-events-user-activated-1-send-welcome-notification.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-user-created-0-create-company
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-user-created-0-create-company
          __path__: /var/log/runall/task-events-user-created-0-create-company.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-user-created-1-send-welcome-email
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-user-created-1-send-welcome-email
          __path__: /var/log/runall/task-events-user-created-1-send-welcome-email.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-user-created-2-sync-user-profile
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-user-created-2-sync-user-profile
          __path__: /var/log/runall/task-events-user-created-2-sync-user-profile.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-wechat-identity-conflict-1-audit-alert
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-wechat-identity-conflict-1-audit-alert
          __path__: /var/log/runall/task-events-wechat-identity-conflict-1-audit-alert.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-wechat-mp-cleanup-1-cleanup
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-wechat-mp-cleanup-1-cleanup
          __path__: /var/log/runall/task-events-wechat-mp-cleanup-1-cleanup.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-workspace-created-1-process-workspace-creation
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-workspace-created-1-process-workspace-creation
          __path__: /var/log/runall/task-events-workspace-created-1-process-workspace-creation.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-events-workspace-machine-idle-1-recycle-idle-nodes
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-events-workspace-machine-idle-1-recycle-idle-nodes
          __path__: /var/log/runall/task-events-workspace-machine-idle-1-recycle-idle-nodes.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-gateway
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-gateway
          __path__: /var/log/runall/task-gateway.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-git-oauth
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-git-oauth
          __path__: /var/log/runall/task-git-oauth.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-project-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-project-service
          __path__: /var/log/runall/task-project-service.log
      - targets:
          - localhost
        labels:
          job: task-project-service
          __path__: /var/log/runall/task-project-service-run.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-referral
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-referral
          __path__: /var/log/runall/task-referral.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-sse
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-sse
          __path__: /var/log/runall/task-sse.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-task-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-task-service
          __path__: /var/log/runall/task-task-service.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: task-tenant-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-tenant-service
          __path__: /var/log/runall/task-tenant-service.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: taskFE
    static_configs:
      - targets:
          - localhost
        labels:
          job: taskFE
          __path__: /var/log/runall/taskFE.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: value-stream
    static_configs:
      - targets:
          - localhost
        labels:
          job: value-stream
          __path__: /var/log/runall/value-stream.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload

  - job_name: wechat-mp-egress
    static_configs:
      - targets:
          - localhost
        labels:
          job: wechat-mp-egress
          __path__: /var/log/runall/wechat-mp-egress.log
    pipeline_stages:
      # Accept both runAll-prefixed lines and raw slog JSON (taskEvents / manual tee).
      - regex:
          expression: '^(?:\S+ \((?:stdout|stderr)\) )?(?P<payload>.*)$'
      - json:
          expressions:
            ts: ts
            level: level
            msg: msg
            service: service
            trace_id: trace_id
            traceId: traceId
          source: payload
      # OPT-20260831-021: 用 JSON 的 ts 作为 Loki 时间戳。Seeked Offset:0 重刮时
      # 存量行不再按摄入时刻写入，Grafana 30m/1h volume 图不会在恢复前假平。
      - timestamp:
          source: ts
          format: RFC3339Nano
      # Plaintext fallback when JSON has no level (e.g. log.Fatalf "Config error: ...").
      - regex:
          expression: '(?i)\b(?P<plain_level>error|fatal|panic|warn(?:ing)?|info|debug)\b'
          source: payload
      - template:
          source: level
          template: '{{ if .level }}{{ .level }}{{ else if eq .plain_level "fatal" }}error{{ else if eq .plain_level "panic" }}error{{ else if eq .plain_level "warning" }}warn{{ else }}{{ .plain_level }}{{ end }}'
      - regex:
          expression: '\[trace_id=(?P<trace_id>[A-Za-z0-9._:-]{8,256})\]'
          source: payload
      - template:
          source: trace_id
          template: '{{ if .trace_id }}{{ .trace_id }}{{ else }}{{ .traceId }}{{ end }}'
      - regex:
          expression: '^[A-Za-z0-9._:-]{8,256}$'
          source: trace_id
      - labels:
          level:
          service:
      - structured_metadata:
          trace_id:
          msg:
      - output:
          source: payload
  - job_name: apisix-gateway
    static_configs:
      - targets:
          - localhost
        labels:
          job: task-gateway
          __path__: /var/log/apisix/access.log
      - targets:
          - localhost
        labels:
          job: task-gateway
          __path__: /var/log/apisix/error.log
      - targets:
          - localhost
        labels:
          job: task-gateway
          __path__: /var/log/apisix/taskgateway-access.log
    pipeline_stages:
      - regex:
          expression: '(?i)(?:x-trace-id|trace_id)["'']?\s*[=:]\s*["'']?(?P<trace_id>[A-Za-z0-9._:-]{8,256})'
      - regex:
          expression: '\[(?P<level>error|warn|crit|alert|emerg|notice|info)\]'
      - labels:
          level:
      - structured_metadata:
          trace_id:
