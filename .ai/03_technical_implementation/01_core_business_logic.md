# 业务逻辑与安全要求

## 基本信息
- 版本：3.21.0
- 创建日期：2026-01-27
- 最后修改：2026-05-08
- 维护者：Trae AI 团队

## 规则分类

### 核心规则

#### 业务逻辑
- 描述：定义项目核心业务逻辑的设计和实现规范
- 适用场景：所有业务逻辑开发
- 优先级：高
- 规则：
  - **必须**采用领域驱动设计（DDD）方式开发，确保项目核心业务逻辑与具体依赖库解耦（强制校验见 `scripts/ci/check_ddd_bdd_compliance.py`）
  - **必须**采用 DDD 的事件驱动方式进行设计，以方便各个业务的解耦
  - 需要考虑面向大数据时的设计, ID上采用雪花算法
  - 禁止使用模拟数据

#### 安全要求
- 描述：定义项目安全相关的实现规范
- 适用场景：所有涉及安全的功能实现
- 优先级：高
- 规则：
  - 项目密码必须前端加密：
    - 适用场景：所有涉及密码传输的场景，包括但不限于用户注册、登录认证、密码重置、密码修改等
    - 加密方式：必须使用安全的加密算法（如SHA-256、AES-256等），禁止使用明文传输或简单哈希（如MD5）
    - 加密时机：在密码数据离开前端应用之前进行加密，确保传输过程中始终为加密状态
    - 加密验证：后端必须验证密码加密状态，拒绝接收未加密的密码数据
    - 前后端配合：前端和后端需约定统一的加密算法、密钥管理机制和数据格式
    - 实现要求：密码输入框必须使用安全输入类型，加密逻辑需封装为独立函数，便于复用和维护

#### 视图HTML内容返回规范
- 描述：视图中不应该直接 hardcode 返回 HttpResponse html 内容，也不要返回模版文件，而是重定向到前端渲染页面
- 适用场景：所有需要返回 HTML 内容的视图函数
- 优先级：高
- 规则：
  - 避免在视图函数中直接拼接 HTML 字符串
  - 避免使用 Django 的模板系统渲染 HTML 内容
  - 对于需要返回 HTML 内容的请求，应该重定向到前端渲染页面
  - 使用 HttpResponseRedirect 进行重定向到前端路由
  - 前端路由应该负责渲染对应的页面内容

## 规则冲突处理
- 当规则冲突时，遵循以下优先级：
  1. 核心规则 &gt; 最佳实践 &gt; 风格指南
  2. 文件级规则 &gt; 目录级规则 &gt; 全局规则
  3. 新版本规则覆盖旧版本规则

## 变更日志
- 2026-05-08：版本 3.21.0 - DDD 业务逻辑规则明确为强制并与 CI 校验对齐
- 2026-03-16：版本 3.20.0 - 从主文件拆分
