---
url: "https://pay.weixin.qq.com/doc/v3/partner/4017327753-go.md"
title: "查询图片生成任务执行结果-请求示例-go"
doc_id: 4017327753
api_version: "apiv3"
product: "商品券（多次优惠）"
http_method: "GET"
request_path: "/v3/marketing/partner/product-coupon/image-generation-tasks/{task_id}"
url_display: "https://pay.weixin.qq.com/doc/v3/partner/4017327753"
title_display: "APIv3/合作伙伴/品牌经营/商品券（多次优惠）/API列表/生成商品券头图/查询图片生成任务执行结果-请求示例-go"
---

## Go


需配合微信支付工具库 wxpay\_utility 使用，请参考 [Go](https://pay.weixin.qq.com/doc/v3/partner/4015119446.md)

```
package main

import (
	"demo/wxpay_utility" // 引用微信支付工具库，参考 https://pay.weixin.qq.com/doc/v3/partner/4015119446
	"encoding/json"
	"fmt"
	"net/http"
	"net/url"
	"strings"
)

func main() {
	// TODO: 请准备商户开发必要参数，参考：https://pay.weixin.qq.com/doc/v3/partner/4013080340
	config, err := wxpay_utility.CreateMchConfig(
		"19xxxxxxxx",                 // 商户号，是由微信支付系统生成并分配给每个商户的唯一标识符，商户号获取方式参考 https://pay.weixin.qq.com/doc/v3/partner/4013080340
		"1DDE55AD98Exxxxxxxxxx",      // 商户API证书序列号，如何获取请参考 https://pay.weixin.qq.com/doc/v3/partner/4013058924
		"/path/to/apiclient_key.pem", // 商户API证书私钥文件路径，本地文件路径
		"PUB_KEY_ID_xxxxxxxxxxxxx",   // 微信支付公钥ID，如何获取请参考 https://pay.weixin.qq.com/doc/v3/partner/4013038589
		"/path/to/wxp_pub.pem",       // 微信支付公钥文件路径，本地文件路径
	)
	if err != nil {
		fmt.Println(err)
		return
	}

	request := &GetImageGenerationTaskRequest{
		BrandId:             wxpay_utility.String("120344"),
		TaskId:              wxpay_utility.String("image_generation_task_1"),
		ImageGenerationType: IMAGEGENERATIONTYPE_COMBINE_IMAGE.Ptr(),
	}

	response, err := GetImageGenerationTask(config, request)
	if err != nil {
		fmt.Printf("请求失败: %+v\n", err)
		// TODO: 请求失败，根据状态码执行不同的处理
		return
	}

	// TODO: 请求成功，继续业务逻辑
	fmt.Printf("请求成功: %+v\n", response)
}

func GetImageGenerationTask(config *wxpay_utility.MchConfig, request *GetImageGenerationTaskRequest) (response *ImageGenerationTask, err error) {
	const (
		host   = "https://api.mch.weixin.qq.com"
		method = "GET"
		path   = "/v3/marketing/partner/product-coupon/image-generation-tasks/{task_id}"
	)

	reqUrl, err := url.Parse(fmt.Sprintf("%s%s", host, path))
	if err != nil {
		return nil, err
	}
	reqUrl.Path = strings.Replace(reqUrl.Path, "{task_id}", url.PathEscape(*request.TaskId), -1)
	query := reqUrl.Query()
	if request.BrandId != nil {
		query.Add("brand_id", *request.BrandId)
	}
	if request.ImageGenerationType != nil {
		query.Add("image_generation_type", fmt.Sprintf("%v", *request.ImageGenerationType))
	}
	reqUrl.RawQuery = query.Encode()
	httpRequest, err := http.NewRequest(method, reqUrl.String(), nil)
	if err != nil {
		return nil, err
	}
	httpRequest.Header.Set("Accept", "application/json")
	httpRequest.Header.Set("Wechatpay-Serial", config.WechatPayPublicKeyId())
	authorization, err := wxpay_utility.BuildAuthorization(config.MchId(), config.CertificateSerialNo(), config.PrivateKey(), method, reqUrl.RequestURI(), nil)
	if err != nil {
		return nil, err
	}
	httpRequest.Header.Set("Authorization", authorization)

	client := &http.Client{}
	httpResponse, err := client.Do(httpRequest)
	if err != nil {
		return nil, err
	}
	respBody, err := wxpay_utility.ExtractResponseBody(httpResponse)
	if err != nil {
		return nil, err
	}
	if httpResponse.StatusCode >= 200 && httpResponse.StatusCode < 300 {
		// 2XX 成功，验证应答签名
		err = wxpay_utility.ValidateResponse(
			config.WechatPayPublicKeyId(),
			config.WechatPayPublicKey(),
			&httpResponse.Header,
			respBody,
		)
		if err != nil {
			return nil, err
		}
		response := &ImageGenerationTask{}
		if err := json.Unmarshal(respBody, response); err != nil {
			return nil, err
		}

		return response, nil
	} else {
		return nil, wxpay_utility.NewApiException(
			httpResponse.StatusCode,
			httpResponse.Header,
			respBody,
		)
	}
}

type GetImageGenerationTaskRequest struct {
	BrandId             *string              `json:"brand_id,omitempty"`
	TaskId              *string              `json:"task_id,omitempty"`
	ImageGenerationType *ImageGenerationType `json:"image_generation_type,omitempty"`
}

func (o *GetImageGenerationTaskRequest) MarshalJSON() ([]byte, error) {
	type Alias GetImageGenerationTaskRequest
	a := &struct {
		BrandId             *string              `json:"brand_id,omitempty"`
		TaskId              *string              `json:"task_id,omitempty"`
		ImageGenerationType *ImageGenerationType `json:"image_generation_type,omitempty"`
		*Alias
	}{
		// 序列化时移除非 Body 字段
		BrandId:             nil,
		TaskId:              nil,
		ImageGenerationType: nil,
		Alias:               (*Alias)(o),
	}
	return json.Marshal(a)
}

type ImageGenerationTask struct {
	BrandId             *string              `json:"brand_id,omitempty"`
	TaskId              *string              `json:"task_id,omitempty"`
	ImageGenerationType *ImageGenerationType `json:"image_generation_type,omitempty"`
	TaskState           *TaskState           `json:"task_state,omitempty"`
	CombineImageResult  *CombineImageResult  `json:"combine_image_result,omitempty"`
	CutOutResult        *CutOutResult        `json:"cut_out_result,omitempty"`
}

type ImageGenerationType string

func (e ImageGenerationType) Ptr() *ImageGenerationType {
	return &e
}

const (
	IMAGEGENERATIONTYPE_COMBINE_IMAGE ImageGenerationType = "COMBINE_IMAGE"
	IMAGEGENERATIONTYPE_CUT_OUT       ImageGenerationType = "CUT_OUT"
)

type TaskState string

func (e TaskState) Ptr() *TaskState {
	return &e
}

const (
	TASKSTATE_PROCESSING TaskState = "PROCESSING"
	TASKSTATE_FAIL       TaskState = "FAIL"
	TASKSTATE_SUCCESS    TaskState = "SUCCESS"
)

type CombineImageResult struct {
	ImageUrl *string `json:"image_url,omitempty"`
}

type CutOutResult struct {
	ImageUrl *string `json:"image_url,omitempty"`
}

```
