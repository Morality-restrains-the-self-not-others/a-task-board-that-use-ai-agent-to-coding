---
url: "https://pay.weixin.qq.com/doc/v3/partner/4016366816-go.md"
title: "根据业务申请编号查询添加申请状态-请求示例-go"
doc_id: 4016366816
api_version: "apiv3"
product: "商家名片"
http_method: "GET"
request_path: "/v3/brand/card/card-links/business-code/{business_code}"
url_display: "https://pay.weixin.qq.com/doc/v3/partner/4016366816"
title_display: "APIv3/合作伙伴/品牌经营/商家名片/API列表/交易连接名片/根据业务申请编号查询添加申请状态-请求示例-go"
---

## Go


需配合微信支付工具库 wxpay\_utility 使用，请参考 [Go](https://pay.weixin.qq.com/doc/v3/partner/4015119446.md)

```
package main

import (
	"demo/wxpay_utility" // 引用微信支付工具库，参考 https://pay.weixin.qq.com/doc/v3/partner/4015119446
	"encoding/json"
	"fmt"
	"net/http"
	"net/url"
	"strings"
)

func main() {
	// TODO: 请准备商户开发必要参数，参考：https://pay.weixin.qq.com/doc/v3/partner/4013080340
	config, err := wxpay_utility.CreateMchConfig(
		"19xxxxxxxx",                 // 商户号，是由微信支付系统生成并分配给每个商户的唯一标识符，商户号获取方式参考 https://pay.weixin.qq.com/doc/v3/partner/4013080340
		"1DDE55AD98Exxxxxxxxxx",      // 商户API证书序列号，如何获取请参考 https://pay.weixin.qq.com/doc/v3/partner/4013058924
		"/path/to/apiclient_key.pem", // 商户API证书私钥文件路径，本地文件路径
		"PUB_KEY_ID_xxxxxxxxxxxxx",   // 微信支付公钥ID，如何获取请参考 https://pay.weixin.qq.com/doc/v3/partner/4013038589
		"/path/to/wxp_pub.pem",       // 微信支付公钥文件路径，本地文件路径
	)
	if err != nil {
		fmt.Println(err)
		return
	}

	request := &GetStateByBusinessCodeRequest{
		BusinessCode: wxpay_utility.String("1900013511_10000"),
		BrandId:      wxpay_utility.String("123456"),
	}

	response, err := GetStateByBusinessCode(config, request)
	if err != nil {
		fmt.Printf("请求失败: %+v\n", err)
		// TODO: 请求失败，根据状态码执行不同的处理
		return
	}

	// TODO: 请求成功，继续业务逻辑
	fmt.Printf("请求成功: %+v\n", response)
}

func GetStateByBusinessCode(config *wxpay_utility.MchConfig, request *GetStateByBusinessCodeRequest) (response *GetStateByBusinessCodeResponse, err error) {
	const (
		host   = "https://api.mch.weixin.qq.com"
		method = "GET"
		path   = "/v3/brand/card/card-links/business-code/{business_code}"
	)

	reqUrl, err := url.Parse(fmt.Sprintf("%s%s", host, path))
	if err != nil {
		return nil, err
	}
	reqUrl.Path = strings.Replace(reqUrl.Path, "{business_code}", url.PathEscape(*request.BusinessCode), -1)
	query := reqUrl.Query()
	if request.BrandId != nil {
		query.Add("brand_id", *request.BrandId)
	}
	reqUrl.RawQuery = query.Encode()
	httpRequest, err := http.NewRequest(method, reqUrl.String(), nil)
	if err != nil {
		return nil, err
	}
	httpRequest.Header.Set("Accept", "application/json")
	httpRequest.Header.Set("Wechatpay-Serial", config.WechatPayPublicKeyId())
	authorization, err := wxpay_utility.BuildAuthorization(config.MchId(), config.CertificateSerialNo(), config.PrivateKey(), method, reqUrl.RequestURI(), nil)
	if err != nil {
		return nil, err
	}
	httpRequest.Header.Set("Authorization", authorization)

	client := &http.Client{}
	httpResponse, err := client.Do(httpRequest)
	if err != nil {
		return nil, err
	}
	respBody, err := wxpay_utility.ExtractResponseBody(httpResponse)
	if err != nil {
		return nil, err
	}
	if httpResponse.StatusCode >= 200 && httpResponse.StatusCode < 300 {
		// 2XX 成功，验证应答签名
		err = wxpay_utility.ValidateResponse(
			config.WechatPayPublicKeyId(),
			config.WechatPayPublicKey(),
			&httpResponse.Header,
			respBody,
		)
		if err != nil {
			return nil, err
		}
		response := &GetStateByBusinessCodeResponse{}
		if err := json.Unmarshal(respBody, response); err != nil {
			return nil, err
		}

		return response, nil
	} else {
		return nil, wxpay_utility.NewApiException(
			httpResponse.StatusCode,
			httpResponse.Header,
			respBody,
		)
	}
}

type GetStateByBusinessCodeRequest struct {
	BusinessCode *string `json:"business_code,omitempty"`
	BrandId      *string `json:"brand_id,omitempty"`
}

func (o *GetStateByBusinessCodeRequest) MarshalJSON() ([]byte, error) {
	type Alias GetStateByBusinessCodeRequest
	a := &struct {
		BusinessCode *string `json:"business_code,omitempty"`
		BrandId      *string `json:"brand_id,omitempty"`
		*Alias
	}{
		// 序列化时移除非 Body 字段
		BusinessCode: nil,
		BrandId:      nil,
		Alias:        (*Alias)(o),
	}
	return json.Marshal(a)
}

type GetStateByBusinessCodeResponse struct {
	BusinessCode       *string             `json:"business_code,omitempty"`
	BrandId            *string             `json:"brand_id,omitempty"`
	PaymentScene       *PaymentScene       `json:"payment_scene,omitempty"`
	Appid              *string             `json:"appid,omitempty"`
	CardLinkMchid      *string             `json:"card_link_mchid,omitempty"`
	ServiceId          *string             `json:"service_id,omitempty"`
	ConfigurationState *ConfigurationState `json:"configuration_state,omitempty"`
	RejectReason       *string             `json:"reject_reason,omitempty"`
}

type PaymentScene string

func (e PaymentScene) Ptr() *PaymentScene {
	return &e
}

const (
	PAYMENTSCENE_MINI_PROGRAM  PaymentScene = "MINI_PROGRAM"
	PAYMENTSCENE_APP           PaymentScene = "APP"
	PAYMENTSCENE_PAYMENT_SCORE PaymentScene = "PAYMENT_SCORE"
	PAYMENTSCENE_PAYMENT_CODE  PaymentScene = "PAYMENT_CODE"
)

type ConfigurationState string

func (e ConfigurationState) Ptr() *ConfigurationState {
	return &e
}

const (
	CONFIGURATIONSTATE_STATE_UNKNOWN         ConfigurationState = "STATE_UNKNOWN"
	CONFIGURATIONSTATE_WAITING_AUDIT         ConfigurationState = "WAITING_AUDIT"
	CONFIGURATIONSTATE_AUDIT_REJECT          ConfigurationState = "AUDIT_REJECT"
	CONFIGURATIONSTATE_WAITING_CONFIRMATION  ConfigurationState = "WAITING_CONFIRMATION"
	CONFIGURATIONSTATE_MERCHANT_ADMIN_REJECT ConfigurationState = "MERCHANT_ADMIN_REJECT"
	CONFIGURATIONSTATE_IN_EFFECT             ConfigurationState = "IN_EFFECT"
	CONFIGURATIONSTATE_CANCELED              ConfigurationState = "CANCELED"
)

```
