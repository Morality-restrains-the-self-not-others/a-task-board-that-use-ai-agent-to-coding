---
url: "https://pay.weixin.qq.com/doc/v3/partner/4016420099-go.md"
title: "商户注销资格校验-请求示例-go"
doc_id: 4016420099
api_version: "apiv3"
product: "注销预校验"
http_method: "GET"
request_path: "/v3/ecommerce/account/apply-cancel-withdraw/validate-cancel/{sub_mchid}"
url_display: "https://pay.weixin.qq.com/doc/v3/partner/4016420099"
title_display: "APIv3/合作伙伴/平台收付通-电商交易解决方案/商户注销/注销预校验/API列表/商户注销资格校验-请求示例-go"
---

## Go


需配合微信支付工具库 wxpay\_utility 使用，请参考 [Go](https://pay.weixin.qq.com/doc/v3/partner/4015119446.md)

```
package main

import (
	"demo/wxpay_utility" // 引用微信支付工具库，参考 https://pay.weixin.qq.com/doc/v3/partner/4015119446
	"encoding/json"
	"fmt"
	"net/http"
	"net/url"
	"strings"
)

func main() {
	// TODO: 请准备商户开发必要参数，参考：https://pay.weixin.qq.com/doc/v3/partner/4013080340
	config, err := wxpay_utility.CreateMchConfig(
		"19xxxxxxxx",                 // 商户号，是由微信支付系统生成并分配给每个商户的唯一标识符，商户号获取方式参考 https://pay.weixin.qq.com/doc/v3/partner/4013080340
		"1DDE55AD98Exxxxxxxxxx",      // 商户API证书序列号，如何获取请参考 https://pay.weixin.qq.com/doc/v3/partner/4013058924
		"/path/to/apiclient_key.pem", // 商户API证书私钥文件路径，本地文件路径
		"PUB_KEY_ID_xxxxxxxxxxxxx",   // 微信支付公钥ID，如何获取请参考 https://pay.weixin.qq.com/doc/v3/partner/4013038589
		"/path/to/wxp_pub.pem",       // 微信支付公钥文件路径，本地文件路径
	)
	if err != nil {
		fmt.Println(err)
		return
	}

	request := &ValidateCancelRequest{
		SubMchid: wxpay_utility.String("1900000109"),
	}

	response, err := ValidateCancel(config, request)
	if err != nil {
		fmt.Printf("请求失败: %+v\n", err)
		// TODO: 请求失败，根据状态码执行不同的处理
		return
	}

	// TODO: 请求成功，继续业务逻辑
	fmt.Printf("请求成功: %+v\n", response)
}

func ValidateCancel(config *wxpay_utility.MchConfig, request *ValidateCancelRequest) (response *ValidateCancelResponse, err error) {
	const (
		host   = "https://api.mch.weixin.qq.com"
		method = "GET"
		path   = "/v3/ecommerce/account/apply-cancel-withdraw/validate-cancel/{sub_mchid}"
	)

	reqUrl, err := url.Parse(fmt.Sprintf("%s%s", host, path))
	if err != nil {
		return nil, err
	}
	reqUrl.Path = strings.Replace(reqUrl.Path, "{sub_mchid}", url.PathEscape(*request.SubMchid), -1)
	httpRequest, err := http.NewRequest(method, reqUrl.String(), nil)
	if err != nil {
		return nil, err
	}
	httpRequest.Header.Set("Accept", "application/json")
	httpRequest.Header.Set("Wechatpay-Serial", config.WechatPayPublicKeyId())
	authorization, err := wxpay_utility.BuildAuthorization(config.MchId(), config.CertificateSerialNo(), config.PrivateKey(), method, reqUrl.RequestURI(), nil)
	if err != nil {
		return nil, err
	}
	httpRequest.Header.Set("Authorization", authorization)

	client := &http.Client{}
	httpResponse, err := client.Do(httpRequest)
	if err != nil {
		return nil, err
	}
	respBody, err := wxpay_utility.ExtractResponseBody(httpResponse)
	if err != nil {
		return nil, err
	}
	if httpResponse.StatusCode >= 200 && httpResponse.StatusCode < 300 {
		// 2XX 成功，验证应答签名
		err = wxpay_utility.ValidateResponse(
			config.WechatPayPublicKeyId(),
			config.WechatPayPublicKey(),
			&httpResponse.Header,
			respBody,
		)
		if err != nil {
			return nil, err
		}
		response := &ValidateCancelResponse{}
		if err := json.Unmarshal(respBody, response); err != nil {
			return nil, err
		}

		return response, nil
	} else {
		return nil, wxpay_utility.NewApiException(
			httpResponse.StatusCode,
			httpResponse.Header,
			respBody,
		)
	}
}

type ValidateCancelRequest struct {
	SubMchid *string `json:"sub_mchid,omitempty"`
}

func (o *ValidateCancelRequest) MarshalJSON() ([]byte, error) {
	type Alias ValidateCancelRequest
	a := &struct {
		SubMchid *string `json:"sub_mchid,omitempty"`
		*Alias
	}{
		// 序列化时移除非 Body 字段
		SubMchid: nil,
		Alias:    (*Alias)(o),
	}
	return json.Marshal(a)
}

type ValidateCancelResponse struct {
	SubMchid       *string                 `json:"sub_mchid,omitempty"`
	MerchantState  *MerchantState          `json:"merchant_state,omitempty"`
	ValidateResult *ValidateCancelResult   `json:"validate_result,omitempty"`
	AccountInfo    []AccountInfo           `json:"account_info,omitempty"`
	BlockReasons   []BlockCancelReasonInfo `json:"block_reasons,omitempty"`
}

type MerchantState string

func (e MerchantState) Ptr() *MerchantState {
	return &e
}

const (
	MERCHANTSTATE_NORMAL             MerchantState = "NORMAL"
	MERCHANTSTATE_HAS_BEEN_CANCELLED MerchantState = "HAS_BEEN_CANCELLED"
)

type ValidateCancelResult string

func (e ValidateCancelResult) Ptr() *ValidateCancelResult {
	return &e
}

const (
	VALIDATECANCELRESULT_ALLOW_CANCEL_WITHDRAW     ValidateCancelResult = "ALLOW_CANCEL_WITHDRAW"
	VALIDATECANCELRESULT_NOT_ALLOW_CANCEL_WITHDRAW ValidateCancelResult = "NOT_ALLOW_CANCEL_WITHDRAW"
)

type AccountInfo struct {
	OutAccountType *OutAccountType `json:"out_account_type,omitempty"`
	Amount         *int64          `json:"amount,omitempty"`
}

type BlockCancelReasonInfo struct {
	Type        *BlockCancelReasonType `json:"type,omitempty"`
	Description *string                `json:"description,omitempty"`
}

type OutAccountType string

func (e OutAccountType) Ptr() *OutAccountType {
	return &e
}

const (
	OUTACCOUNTTYPE_BASIC_ACCOUNT     OutAccountType = "BASIC_ACCOUNT"
	OUTACCOUNTTYPE_OPERATE_ACCOUNT   OutAccountType = "OPERATE_ACCOUNT"
	OUTACCOUNTTYPE_MARGIN_ACCOUNT    OutAccountType = "MARGIN_ACCOUNT"
	OUTACCOUNTTYPE_TRADE_FEE_ACCOUNT OutAccountType = "TRADE_FEE_ACCOUNT"
)

type BlockCancelReasonType string

func (e BlockCancelReasonType) Ptr() *BlockCancelReasonType {
	return &e
}

const (
	BLOCKCANCELREASONTYPE_CONSUMER_COMPLAINT_UNPROCESSED BlockCancelReasonType = "CONSUMER_COMPLAINT_UNPROCESSED"
	BLOCKCANCELREASONTYPE_HAS_BLOCKING_CONTROL           BlockCancelReasonType = "HAS_BLOCKING_CONTROL"
	BLOCKCANCELREASONTYPE_FUNDS_PENDING_PROCESSING       BlockCancelReasonType = "FUNDS_PENDING_PROCESSING"
	BLOCKCANCELREASONTYPE_OTHER_REASON                   BlockCancelReasonType = "OTHER_REASON"
)

```
