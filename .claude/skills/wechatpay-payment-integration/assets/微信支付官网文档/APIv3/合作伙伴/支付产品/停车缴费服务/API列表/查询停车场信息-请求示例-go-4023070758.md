---
url: "https://pay.weixin.qq.com/doc/v3/partner/4023070758-go.md"
title: "查询停车场信息-请求示例-go"
doc_id: 4023070758
api_version: "apiv3"
product: "停车缴费服务"
http_method: "GET"
request_path: "/v3/parking/reminders/parking-lot"
url_display: "https://pay.weixin.qq.com/doc/v3/partner/4023070758"
title_display: "APIv3/合作伙伴/支付产品/停车缴费服务/API列表/查询停车场信息-请求示例-go"
---

## Go


需配合微信支付工具库 wxpay\_utility 使用，请参考 [Go](https://pay.weixin.qq.com/doc/v3/partner/4015119446.md)

```
package main

import (
	"demo/wxpay_utility" // 引用微信支付工具库，参考 https://pay.weixin.qq.com/doc/v3/partner/4015119446
	"encoding/json"
	"fmt"
	"net/http"
	"net/url"
)

func main() {
	// TODO: 请准备商户开发必要参数，参考：https://pay.weixin.qq.com/doc/v3/partner/4013080340
	config, err := wxpay_utility.CreateMchConfig(
		"19xxxxxxxx",                 // 商户号，是由微信支付系统生成并分配给每个商户的唯一标识符，商户号获取方式参考 https://pay.weixin.qq.com/doc/v3/partner/4013080340
		"1DDE55AD98Exxxxxxxxxx",      // 商户API证书序列号，如何获取请参考 https://pay.weixin.qq.com/doc/v3/partner/4013058924
		"/path/to/apiclient_key.pem", // 商户API证书私钥文件路径，本地文件路径
		"PUB_KEY_ID_xxxxxxxxxxxxx",   // 微信支付公钥ID，如何获取请参考 https://pay.weixin.qq.com/doc/v3/partner/4013038589
		"/path/to/wxp_pub.pem",       // 微信支付公钥文件路径，本地文件路径
	)
	if err != nil {
		fmt.Println(err)
		return
	}

	request := &QueryParkingLotRequest{
		OutParkingLotId: wxpay_utility.String("lot_sz_tencent_001"),
		WxParkingLotId:  wxpay_utility.String("21232735744117624001123604298240"),
	}

	response, err := QueryParkingLot(config, request)
	if err != nil {
		fmt.Printf("请求失败: %+v\n", err)
		// TODO: 请求失败，根据状态码执行不同的处理
		return
	}

	// TODO: 请求成功，继续业务逻辑
	fmt.Printf("请求成功: %+v\n", response)
}

func QueryParkingLot(config *wxpay_utility.MchConfig, request *QueryParkingLotRequest) (response *QueryParkingLotResponse, err error) {
	const (
		host   = "https://api.mch.weixin.qq.com"
		method = "GET"
		path   = "/v3/parking/reminders/parking-lot"
	)

	reqUrl, err := url.Parse(fmt.Sprintf("%s%s", host, path))
	if err != nil {
		return nil, err
	}
	query := reqUrl.Query()
	if request.OutParkingLotId != nil {
		query.Add("out_parking_lot_id", *request.OutParkingLotId)
	}
	if request.WxParkingLotId != nil {
		query.Add("wx_parking_lot_id", *request.WxParkingLotId)
	}
	reqUrl.RawQuery = query.Encode()
	httpRequest, err := http.NewRequest(method, reqUrl.String(), nil)
	if err != nil {
		return nil, err
	}
	httpRequest.Header.Set("Accept", "application/json")
	httpRequest.Header.Set("Wechatpay-Serial", config.WechatPayPublicKeyId())
	authorization, err := wxpay_utility.BuildAuthorization(config.MchId(), config.CertificateSerialNo(), config.PrivateKey(), method, reqUrl.RequestURI(), nil)
	if err != nil {
		return nil, err
	}
	httpRequest.Header.Set("Authorization", authorization)

	client := &http.Client{}
	httpResponse, err := client.Do(httpRequest)
	if err != nil {
		return nil, err
	}
	respBody, err := wxpay_utility.ExtractResponseBody(httpResponse)
	if err != nil {
		return nil, err
	}
	if httpResponse.StatusCode >= 200 && httpResponse.StatusCode < 300 {
		// 2XX 成功，验证应答签名
		err = wxpay_utility.ValidateResponse(
			config.WechatPayPublicKeyId(),
			config.WechatPayPublicKey(),
			&httpResponse.Header,
			respBody,
		)
		if err != nil {
			return nil, err
		}
		response := &QueryParkingLotResponse{}
		if err := json.Unmarshal(respBody, response); err != nil {
			return nil, err
		}

		return response, nil
	} else {
		return nil, wxpay_utility.NewApiException(
			httpResponse.StatusCode,
			httpResponse.Header,
			respBody,
		)
	}
}

type QueryParkingLotRequest struct {
	OutParkingLotId *string `json:"out_parking_lot_id,omitempty"`
	WxParkingLotId  *string `json:"wx_parking_lot_id,omitempty"`
}

func (o *QueryParkingLotRequest) MarshalJSON() ([]byte, error) {
	type Alias QueryParkingLotRequest
	a := &struct {
		OutParkingLotId *string `json:"out_parking_lot_id,omitempty"`
		WxParkingLotId  *string `json:"wx_parking_lot_id,omitempty"`
		*Alias
	}{
		// 序列化时移除非 Body 字段
		OutParkingLotId: nil,
		WxParkingLotId:  nil,
		Alias:           (*Alias)(o),
	}
	return json.Marshal(a)
}

type QueryParkingLotResponse struct {
	Code                      *string                 `json:"code,omitempty"`
	Message                   *string                 `json:"message,omitempty"`
	WxParkingLotId            *string                 `json:"wx_parking_lot_id,omitempty"`
	OutParkingLotId           *string                 `json:"out_parking_lot_id,omitempty"`
	ParkingLotName            *string                 `json:"parking_lot_name,omitempty"`
	ParkingLotAddress         *string                 `json:"parking_lot_address,omitempty"`
	PaymentMiniProgAppid      *string                 `json:"payment_mini_prog_appid,omitempty"`
	PaymentMiniProgPath       *string                 `json:"payment_mini_prog_path,omitempty"`
	ParkingOrderMiniProgAppid *string                 `json:"parking_order_mini_prog_appid,omitempty"`
	ParkingOrderMiniProgPath  *string                 `json:"parking_order_mini_prog_path,omitempty"`
	EnabledState              *ParkingLotEnabledState `json:"enabled_state,omitempty"`
	Reason                    *string                 `json:"reason,omitempty"`
}

type ParkingLotEnabledState string

func (e ParkingLotEnabledState) Ptr() *ParkingLotEnabledState {
	return &e
}

const (
	PARKINGLOTENABLEDSTATE_ENABLED_STATE_UNKNOWN     ParkingLotEnabledState = "ENABLED_STATE_UNKNOWN"
	PARKINGLOTENABLEDSTATE_ENABLED_STATE_NOT_ENABLED ParkingLotEnabledState = "ENABLED_STATE_NOT_ENABLED"
	PARKINGLOTENABLEDSTATE_ENABLED_STATE_ENABLED     ParkingLotEnabledState = "ENABLED_STATE_ENABLED"
	PARKINGLOTENABLEDSTATE_ENABLED_STATE_OFFLINE     ParkingLotEnabledState = "ENABLED_STATE_OFFLINE"
)

```
