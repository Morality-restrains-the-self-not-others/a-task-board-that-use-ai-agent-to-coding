---
url: "https://pay.weixin.qq.com/doc/v3/partner/4019557825-go.md"
title: "查询停车费用-请求示例-go"
doc_id: 4019557825
api_version: "apiv3"
product: "停车缴费服务"
http_method: "POST"
request_path: "/v3/parking/reminders/parking-fee"
url_display: "https://pay.weixin.qq.com/doc/v3/partner/4019557825"
title_display: "APIv3/合作伙伴/支付产品/停车缴费服务/API列表/查询停车费用-请求示例-go"
---

## Go


需配合微信支付工具库 wxpay\_utility 使用，请参考 [Go](https://pay.weixin.qq.com/doc/v3/partner/4015119446.md)

```
package main

import (
	"bytes"
	"demo/wxpay_utility" // 引用微信支付工具库，参考 https://pay.weixin.qq.com/doc/v3/partner/4015119446
	"encoding/json"
	"fmt"
	"net/http"
	"net/url"
)

func main() {
	// TODO: 请准备商户开发必要参数，参考：https://pay.weixin.qq.com/doc/v3/partner/4013080340
	config, err := wxpay_utility.CreateMchConfig(
		"19xxxxxxxx",                 // 商户号，是由微信支付系统生成并分配给每个商户的唯一标识符，商户号获取方式参考 https://pay.weixin.qq.com/doc/v3/partner/4013080340
		"1DDE55AD98Exxxxxxxxxx",      // 商户API证书序列号，如何获取请参考 https://pay.weixin.qq.com/doc/v3/partner/4013058924
		"/path/to/apiclient_key.pem", // 商户API证书私钥文件路径，本地文件路径
		"PUB_KEY_ID_xxxxxxxxxxxxx",   // 微信支付公钥ID，如何获取请参考 https://pay.weixin.qq.com/doc/v3/partner/4013038589
		"/path/to/wxp_pub.pem",       // 微信支付公钥文件路径，本地文件路径
	)
	if err != nil {
		fmt.Println(err)
		return
	}

	request := &QueryParkingFeeRequest{
		OutSerialNumber: wxpay_utility.String("PARK20260423001"),
		PlateNumber:     wxpay_utility.String("粤B12345"),
		OutParkingId:    wxpay_utility.String("lot_sz_tencent_001"),
	}

	response, err := QueryParkingFee(config, request)
	if err != nil {
		fmt.Printf("请求失败: %+v\n", err)
		// TODO: 请求失败，根据状态码执行不同的处理
		return
	}

	// TODO: 请求成功，继续业务逻辑
	fmt.Printf("请求成功: %+v\n", response)
}

func QueryParkingFee(config *wxpay_utility.MchConfig, request *QueryParkingFeeRequest) (response *QueryParkingFeeResponse, err error) {
	const (
		host   = "https://api.mch.weixin.qq.com"
		method = "POST"
		path   = "/v3/parking/reminders/parking-fee"
	)

	reqUrl, err := url.Parse(fmt.Sprintf("%s%s", host, path))
	if err != nil {
		return nil, err
	}
	reqBody, err := json.Marshal(request)
	if err != nil {
		return nil, err
	}
	httpRequest, err := http.NewRequest(method, reqUrl.String(), bytes.NewReader(reqBody))
	if err != nil {
		return nil, err
	}
	httpRequest.Header.Set("Accept", "application/json")
	httpRequest.Header.Set("Wechatpay-Serial", config.WechatPayPublicKeyId())
	httpRequest.Header.Set("Content-Type", "application/json")
	authorization, err := wxpay_utility.BuildAuthorization(config.MchId(), config.CertificateSerialNo(), config.PrivateKey(), method, reqUrl.RequestURI(), reqBody)
	if err != nil {
		return nil, err
	}
	httpRequest.Header.Set("Authorization", authorization)

	client := &http.Client{}
	httpResponse, err := client.Do(httpRequest)
	if err != nil {
		return nil, err
	}
	respBody, err := wxpay_utility.ExtractResponseBody(httpResponse)
	if err != nil {
		return nil, err
	}
	if httpResponse.StatusCode >= 200 && httpResponse.StatusCode < 300 {
		// 2XX 成功，验证应答签名
		err = wxpay_utility.ValidateResponse(
			config.WechatPayPublicKeyId(),
			config.WechatPayPublicKey(),
			&httpResponse.Header,
			respBody,
		)
		if err != nil {
			return nil, err
		}
		response := &QueryParkingFeeResponse{}
		if err := json.Unmarshal(respBody, response); err != nil {
			return nil, err
		}

		return response, nil
	} else {
		return nil, wxpay_utility.NewApiException(
			httpResponse.StatusCode,
			httpResponse.Header,
			respBody,
		)
	}
}

type QueryParkingFeeRequest struct {
	OutSerialNumber *string `json:"out_serial_number,omitempty"`
	PlateNumber     *string `json:"plate_number,omitempty"`
	OutParkingId    *string `json:"out_parking_id,omitempty"`
}

type QueryParkingFeeResponse struct {
	Code                 *string       `json:"code,omitempty"`
	Message              *string       `json:"message,omitempty"`
	TotalAmount          *int64        `json:"total_amount,omitempty"`
	ParkingTimestamp     *int64        `json:"parking_timestamp,omitempty"`
	ParkingState         *ParkingState `json:"parking_state,omitempty"`
	PayState             *PayStatus    `json:"pay_state,omitempty"`
	AllowedExitTimestamp *int64        `json:"allowed_exit_timestamp,omitempty"`
	NextRaisePrice       *int64        `json:"next_raise_price,omitempty"`
	NextRaiseTimestamp   *int64        `json:"next_raise_timestamp,omitempty"`
	PayableAmount        *int64        `json:"payable_amount,omitempty"`
	PaidAmount           *int64        `json:"paid_amount,omitempty"`
}

type ParkingState string

func (e ParkingState) Ptr() *ParkingState {
	return &e
}

const (
	PARKINGSTATE_PARKING_STATE_UNKNOWN          ParkingState = "PARKING_STATE_UNKNOWN"
	PARKINGSTATE_PARKING_STATE_ENTERED          ParkingState = "PARKING_STATE_ENTERED"
	PARKINGSTATE_PARKING_STATE_EXIT_GATE        ParkingState = "PARKING_STATE_EXIT_GATE"
	PARKINGSTATE_PARKING_STATE_EXITED           ParkingState = "PARKING_STATE_EXITED"
	PARKINGSTATE_PARKING_STATE_UNEXPECTED_CLOSE ParkingState = "PARKING_STATE_UNEXPECTED_CLOSE"
)

type PayStatus string

func (e PayStatus) Ptr() *PayStatus {
	return &e
}

const (
	PAYSTATUS_PAY_STATUS_UNKNOWN PayStatus = "PAY_STATUS_UNKNOWN"
	PAYSTATUS_PAY_STATUS_UNPAID  PayStatus = "PAY_STATUS_UNPAID"
	PAYSTATUS_PAY_STATUS_PAID    PayStatus = "PAY_STATUS_PAID"
)

```
