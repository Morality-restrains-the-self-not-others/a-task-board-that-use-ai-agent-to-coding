---
url: "https://pay.weixin.qq.com/doc/v3/merchant/4012467251-go.md"
title: "图片请求接口-请求示例-go"
doc_id: 4012467251
api_version: "apiv3"
product: "消费者投诉2.0"
http_method: "GET"
request_path: "/v3/merchant-service/images/{media_id}"
url_display: "https://pay.weixin.qq.com/doc/v3/merchant/4012467251"
title_display: "APIv3/普通商户/交易支持产品/消费者投诉2.0/API列表/商户反馈图片/图片请求接口-请求示例-go"
---

## Go


需配合微信支付工具库 wxpay\_utility 使用，请参考 [Go](https://pay.weixin.qq.com/doc/v3/merchant/4015119334.md)

```
package main

import (
	"demo/wxpay_utility" // 引用微信支付工具库，参考 https://pay.weixin.qq.com/doc/v3/merchant/4015119334
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/url"
	"strings"
)

func main() {
	// TODO: 请准备商户开发必要参数，参考：https://pay.weixin.qq.com/doc/v3/merchant/4013070756
	config, err := wxpay_utility.CreateMchConfig(
		"19xxxxxxxx",                 // 商户号，是由微信支付系统生成并分配给每个商户的唯一标识符，商户号获取方式参考 https://pay.weixin.qq.com/doc/v3/merchant/4013070756
		"1DDE55AD98Exxxxxxxxxx",      // 商户API证书序列号，如何获取请参考 https://pay.weixin.qq.com/doc/v3/merchant/4013053053
		"/path/to/apiclient_key.pem", // 商户API证书私钥文件路径，本地文件路径
		"PUB_KEY_ID_xxxxxxxxxxxxx",   // 微信支付公钥ID，如何获取请参考 https://pay.weixin.qq.com/doc/v3/merchant/4013038816
		"/path/to/wxp_pub.pem",       // 微信支付公钥文件路径，本地文件路径
	)
	if err != nil {
		fmt.Println(err)
		return
	}

	request := &QueryImagesRequest{
		MediaId: wxpay_utility.String("ChsyMDAwMDAwMjAyNDExMDgxNTAyMjQ4MTU1MTgYBSCIk8y5BigBMAE4AQ%3D%3D"),
	}

	response, err := QueryImages(config, request)
	if err != nil {
		fmt.Printf("请求失败: %+v\n", err)
		// TODO: 请求失败，根据状态码执行不同的处理
		return
	}

	// TODO: 请求成功，继续业务逻辑
	fmt.Printf("请求成功: %+v\n", response)
}

func QueryImages(config *wxpay_utility.MchConfig, request *QueryImagesRequest) (response io.ReadCloser, err error) {
	const (
		host   = "https://api.mch.weixin.qq.com"
		method = "GET"
		path   = "/v3/merchant-service/images/{media_id}"
	)

	reqUrl, err := url.Parse(fmt.Sprintf("%s%s", host, path))
	if err != nil {
		return nil, err
	}
	reqUrl.Path = strings.Replace(reqUrl.Path, "{media_id}", url.PathEscape(*request.MediaId), -1)
	httpRequest, err := http.NewRequest(method, reqUrl.String(), nil)
	if err != nil {
		return nil, err
	}
	httpRequest.Header.Set("Accept", "*/*")
	httpRequest.Header.Set("Wechatpay-Serial", config.WechatPayPublicKeyId())
	authorization, err := wxpay_utility.BuildAuthorization(config.MchId(), config.CertificateSerialNo(), config.PrivateKey(), method, reqUrl.RequestURI(), nil)
	if err != nil {
		return nil, err
	}
	httpRequest.Header.Set("Authorization", authorization)

	client := &http.Client{}
	httpResponse, err := client.Do(httpRequest)
	if err != nil {
		return nil, err
	}
	if httpResponse.StatusCode >= 200 && httpResponse.StatusCode < 300 {
		if httpResponse.Body == nil {
			return nil, fmt.Errorf("response body is nil")
		}
		// 注意：调用方在使用完返回的 io.ReadCloser 后必须调用 Close() 方法
		return httpResponse.Body, nil
	} else {
		defer httpResponse.Body.Close()
		respBody, err := wxpay_utility.ExtractResponseBody(httpResponse)
		if err != nil {
			return nil, err
		}
		return nil, wxpay_utility.NewApiException(
			httpResponse.StatusCode,
			httpResponse.Header,
			respBody,
		)
	}
}

type QueryImagesRequest struct {
	MediaId *string `json:"media_id,omitempty"`
}

func (o *QueryImagesRequest) MarshalJSON() ([]byte, error) {
	type Alias QueryImagesRequest
	a := &struct {
		MediaId *string `json:"media_id,omitempty"`
		*Alias
	}{
		// 序列化时移除非 Body 字段
		MediaId: nil,
		Alias:   (*Alias)(o),
	}
	return json.Marshal(a)
}

```
